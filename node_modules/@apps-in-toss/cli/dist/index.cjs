"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// ../../../.yarn/__virtual__/tsup-virtual-5fd798e2dc/0/cache/tsup-npm-8.3.5-ed25596739-7794953cbc.zip/node_modules/tsup/assets/cjs_shims.js
var init_cjs_shims = __esm({
  "../../../.yarn/__virtual__/tsup-virtual-5fd798e2dc/0/cache/tsup-npm-8.3.5-ed25596739-7794953cbc.zip/node_modules/tsup/assets/cjs_shims.js"() {
    "use strict";
  }
});

// ../../../.yarn/cache/picocolors-npm-1.1.1-b0c45fa35e-e2e3e8170a.zip/node_modules/picocolors/picocolors.js
var require_picocolors = __commonJS({
  "../../../.yarn/cache/picocolors-npm-1.1.1-b0c45fa35e-e2e3e8170a.zip/node_modules/picocolors/picocolors.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var p8 = process || {};
    var argv = p8.argv || [];
    var env = p8.env || {};
    var isColorSupported = !(!!env.NO_COLOR || argv.includes("--no-color")) && (!!env.FORCE_COLOR || argv.includes("--color") || p8.platform === "win32" || (p8.stdout || {}).isTTY && env.TERM !== "dumb" || !!env.CI);
    var formatter = (open, close, replace = open) => (input) => {
      let string = "" + input, index = string.indexOf(close, open.length);
      return ~index ? open + replaceClose(string, close, replace, index) + close : open + string + close;
    };
    var replaceClose = (string, close, replace, index) => {
      let result = "", cursor = 0;
      do {
        result += string.substring(cursor, index) + replace;
        cursor = index + close.length;
        index = string.indexOf(close, cursor);
      } while (~index);
      return result + string.substring(cursor);
    };
    var createColors = (enabled = isColorSupported) => {
      let f = enabled ? formatter : () => String;
      return {
        isColorSupported: enabled,
        reset: f("\x1B[0m", "\x1B[0m"),
        bold: f("\x1B[1m", "\x1B[22m", "\x1B[22m\x1B[1m"),
        dim: f("\x1B[2m", "\x1B[22m", "\x1B[22m\x1B[2m"),
        italic: f("\x1B[3m", "\x1B[23m"),
        underline: f("\x1B[4m", "\x1B[24m"),
        inverse: f("\x1B[7m", "\x1B[27m"),
        hidden: f("\x1B[8m", "\x1B[28m"),
        strikethrough: f("\x1B[9m", "\x1B[29m"),
        black: f("\x1B[30m", "\x1B[39m"),
        red: f("\x1B[31m", "\x1B[39m"),
        green: f("\x1B[32m", "\x1B[39m"),
        yellow: f("\x1B[33m", "\x1B[39m"),
        blue: f("\x1B[34m", "\x1B[39m"),
        magenta: f("\x1B[35m", "\x1B[39m"),
        cyan: f("\x1B[36m", "\x1B[39m"),
        white: f("\x1B[37m", "\x1B[39m"),
        gray: f("\x1B[90m", "\x1B[39m"),
        bgBlack: f("\x1B[40m", "\x1B[49m"),
        bgRed: f("\x1B[41m", "\x1B[49m"),
        bgGreen: f("\x1B[42m", "\x1B[49m"),
        bgYellow: f("\x1B[43m", "\x1B[49m"),
        bgBlue: f("\x1B[44m", "\x1B[49m"),
        bgMagenta: f("\x1B[45m", "\x1B[49m"),
        bgCyan: f("\x1B[46m", "\x1B[49m"),
        bgWhite: f("\x1B[47m", "\x1B[49m"),
        blackBright: f("\x1B[90m", "\x1B[39m"),
        redBright: f("\x1B[91m", "\x1B[39m"),
        greenBright: f("\x1B[92m", "\x1B[39m"),
        yellowBright: f("\x1B[93m", "\x1B[39m"),
        blueBright: f("\x1B[94m", "\x1B[39m"),
        magentaBright: f("\x1B[95m", "\x1B[39m"),
        cyanBright: f("\x1B[96m", "\x1B[39m"),
        whiteBright: f("\x1B[97m", "\x1B[39m"),
        bgBlackBright: f("\x1B[100m", "\x1B[49m"),
        bgRedBright: f("\x1B[101m", "\x1B[49m"),
        bgGreenBright: f("\x1B[102m", "\x1B[49m"),
        bgYellowBright: f("\x1B[103m", "\x1B[49m"),
        bgBlueBright: f("\x1B[104m", "\x1B[49m"),
        bgMagentaBright: f("\x1B[105m", "\x1B[49m"),
        bgCyanBright: f("\x1B[106m", "\x1B[49m"),
        bgWhiteBright: f("\x1B[107m", "\x1B[49m")
      };
    };
    module2.exports = createColors();
    module2.exports.createColors = createColors;
  }
});

// ../../../.yarn/cache/ms-npm-2.1.3-eae95409bd-d924b57e73.zip/node_modules/ms/index.js
var require_ms = __commonJS({
  "../../../.yarn/cache/ms-npm-2.1.3-eae95409bd-d924b57e73.zip/node_modules/ms/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var s = 1e3;
    var m = s * 60;
    var h = m * 60;
    var d = h * 24;
    var w = d * 7;
    var y = d * 365.25;
    module2.exports = function(val, options) {
      options = options || {};
      var type = typeof val;
      if (type === "string" && val.length > 0) {
        return parse(val);
      } else if (type === "number" && isFinite(val)) {
        return options.long ? fmtLong(val) : fmtShort(val);
      }
      throw new Error(
        "val is not a non-empty string or a valid number. val=" + JSON.stringify(val)
      );
    };
    function parse(str) {
      str = String(str);
      if (str.length > 100) {
        return;
      }
      var match = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(
        str
      );
      if (!match) {
        return;
      }
      var n = parseFloat(match[1]);
      var type = (match[2] || "ms").toLowerCase();
      switch (type) {
        case "years":
        case "year":
        case "yrs":
        case "yr":
        case "y":
          return n * y;
        case "weeks":
        case "week":
        case "w":
          return n * w;
        case "days":
        case "day":
        case "d":
          return n * d;
        case "hours":
        case "hour":
        case "hrs":
        case "hr":
        case "h":
          return n * h;
        case "minutes":
        case "minute":
        case "mins":
        case "min":
        case "m":
          return n * m;
        case "seconds":
        case "second":
        case "secs":
        case "sec":
        case "s":
          return n * s;
        case "milliseconds":
        case "millisecond":
        case "msecs":
        case "msec":
        case "ms":
          return n;
        default:
          return void 0;
      }
    }
    function fmtShort(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d) {
        return Math.round(ms / d) + "d";
      }
      if (msAbs >= h) {
        return Math.round(ms / h) + "h";
      }
      if (msAbs >= m) {
        return Math.round(ms / m) + "m";
      }
      if (msAbs >= s) {
        return Math.round(ms / s) + "s";
      }
      return ms + "ms";
    }
    function fmtLong(ms) {
      var msAbs = Math.abs(ms);
      if (msAbs >= d) {
        return plural(ms, msAbs, d, "day");
      }
      if (msAbs >= h) {
        return plural(ms, msAbs, h, "hour");
      }
      if (msAbs >= m) {
        return plural(ms, msAbs, m, "minute");
      }
      if (msAbs >= s) {
        return plural(ms, msAbs, s, "second");
      }
      return ms + " ms";
    }
    function plural(ms, msAbs, n, name) {
      var isPlural = msAbs >= n * 1.5;
      return Math.round(ms / n) + " " + name + (isPlural ? "s" : "");
    }
  }
});

// ../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/common.js
var require_common = __commonJS({
  "../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/common.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    function setup(env) {
      createDebug.debug = createDebug;
      createDebug.default = createDebug;
      createDebug.coerce = coerce;
      createDebug.disable = disable;
      createDebug.enable = enable;
      createDebug.enabled = enabled;
      createDebug.humanize = require_ms();
      createDebug.destroy = destroy;
      Object.keys(env).forEach((key) => {
        createDebug[key] = env[key];
      });
      createDebug.names = [];
      createDebug.skips = [];
      createDebug.formatters = {};
      function selectColor(namespace) {
        let hash = 0;
        for (let i = 0; i < namespace.length; i++) {
          hash = (hash << 5) - hash + namespace.charCodeAt(i);
          hash |= 0;
        }
        return createDebug.colors[Math.abs(hash) % createDebug.colors.length];
      }
      createDebug.selectColor = selectColor;
      function createDebug(namespace) {
        let prevTime;
        let enableOverride = null;
        let namespacesCache;
        let enabledCache;
        function debug5(...args) {
          if (!debug5.enabled) {
            return;
          }
          const self = debug5;
          const curr = Number(/* @__PURE__ */ new Date());
          const ms = curr - (prevTime || curr);
          self.diff = ms;
          self.prev = prevTime;
          self.curr = curr;
          prevTime = curr;
          args[0] = createDebug.coerce(args[0]);
          if (typeof args[0] !== "string") {
            args.unshift("%O");
          }
          let index = 0;
          args[0] = args[0].replace(/%([a-zA-Z%])/g, (match, format) => {
            if (match === "%%") {
              return "%";
            }
            index++;
            const formatter = createDebug.formatters[format];
            if (typeof formatter === "function") {
              const val = args[index];
              match = formatter.call(self, val);
              args.splice(index, 1);
              index--;
            }
            return match;
          });
          createDebug.formatArgs.call(self, args);
          const logFn = self.log || createDebug.log;
          logFn.apply(self, args);
        }
        debug5.namespace = namespace;
        debug5.useColors = createDebug.useColors();
        debug5.color = createDebug.selectColor(namespace);
        debug5.extend = extend;
        debug5.destroy = createDebug.destroy;
        Object.defineProperty(debug5, "enabled", {
          enumerable: true,
          configurable: false,
          get: () => {
            if (enableOverride !== null) {
              return enableOverride;
            }
            if (namespacesCache !== createDebug.namespaces) {
              namespacesCache = createDebug.namespaces;
              enabledCache = createDebug.enabled(namespace);
            }
            return enabledCache;
          },
          set: (v) => {
            enableOverride = v;
          }
        });
        if (typeof createDebug.init === "function") {
          createDebug.init(debug5);
        }
        return debug5;
      }
      function extend(namespace, delimiter) {
        const newDebug = createDebug(this.namespace + (typeof delimiter === "undefined" ? ":" : delimiter) + namespace);
        newDebug.log = this.log;
        return newDebug;
      }
      function enable(namespaces) {
        createDebug.save(namespaces);
        createDebug.namespaces = namespaces;
        createDebug.names = [];
        createDebug.skips = [];
        const split = (typeof namespaces === "string" ? namespaces : "").trim().replace(" ", ",").split(",").filter(Boolean);
        for (const ns of split) {
          if (ns[0] === "-") {
            createDebug.skips.push(ns.slice(1));
          } else {
            createDebug.names.push(ns);
          }
        }
      }
      function matchesTemplate(search, template) {
        let searchIndex = 0;
        let templateIndex = 0;
        let starIndex = -1;
        let matchIndex = 0;
        while (searchIndex < search.length) {
          if (templateIndex < template.length && (template[templateIndex] === search[searchIndex] || template[templateIndex] === "*")) {
            if (template[templateIndex] === "*") {
              starIndex = templateIndex;
              matchIndex = searchIndex;
              templateIndex++;
            } else {
              searchIndex++;
              templateIndex++;
            }
          } else if (starIndex !== -1) {
            templateIndex = starIndex + 1;
            matchIndex++;
            searchIndex = matchIndex;
          } else {
            return false;
          }
        }
        while (templateIndex < template.length && template[templateIndex] === "*") {
          templateIndex++;
        }
        return templateIndex === template.length;
      }
      function disable() {
        const namespaces = [
          ...createDebug.names,
          ...createDebug.skips.map((namespace) => "-" + namespace)
        ].join(",");
        createDebug.enable("");
        return namespaces;
      }
      function enabled(name) {
        for (const skip of createDebug.skips) {
          if (matchesTemplate(name, skip)) {
            return false;
          }
        }
        for (const ns of createDebug.names) {
          if (matchesTemplate(name, ns)) {
            return true;
          }
        }
        return false;
      }
      function coerce(val) {
        if (val instanceof Error) {
          return val.stack || val.message;
        }
        return val;
      }
      function destroy() {
        console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
      }
      createDebug.enable(createDebug.load());
      return createDebug;
    }
    module2.exports = setup;
  }
});

// ../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/browser.js
var require_browser = __commonJS({
  "../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/browser.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    exports2.formatArgs = formatArgs;
    exports2.save = save;
    exports2.load = load;
    exports2.useColors = useColors;
    exports2.storage = localstorage();
    exports2.destroy = /* @__PURE__ */ (() => {
      let warned = false;
      return () => {
        if (!warned) {
          warned = true;
          console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
        }
      };
    })();
    exports2.colors = [
      "#0000CC",
      "#0000FF",
      "#0033CC",
      "#0033FF",
      "#0066CC",
      "#0066FF",
      "#0099CC",
      "#0099FF",
      "#00CC00",
      "#00CC33",
      "#00CC66",
      "#00CC99",
      "#00CCCC",
      "#00CCFF",
      "#3300CC",
      "#3300FF",
      "#3333CC",
      "#3333FF",
      "#3366CC",
      "#3366FF",
      "#3399CC",
      "#3399FF",
      "#33CC00",
      "#33CC33",
      "#33CC66",
      "#33CC99",
      "#33CCCC",
      "#33CCFF",
      "#6600CC",
      "#6600FF",
      "#6633CC",
      "#6633FF",
      "#66CC00",
      "#66CC33",
      "#9900CC",
      "#9900FF",
      "#9933CC",
      "#9933FF",
      "#99CC00",
      "#99CC33",
      "#CC0000",
      "#CC0033",
      "#CC0066",
      "#CC0099",
      "#CC00CC",
      "#CC00FF",
      "#CC3300",
      "#CC3333",
      "#CC3366",
      "#CC3399",
      "#CC33CC",
      "#CC33FF",
      "#CC6600",
      "#CC6633",
      "#CC9900",
      "#CC9933",
      "#CCCC00",
      "#CCCC33",
      "#FF0000",
      "#FF0033",
      "#FF0066",
      "#FF0099",
      "#FF00CC",
      "#FF00FF",
      "#FF3300",
      "#FF3333",
      "#FF3366",
      "#FF3399",
      "#FF33CC",
      "#FF33FF",
      "#FF6600",
      "#FF6633",
      "#FF9900",
      "#FF9933",
      "#FFCC00",
      "#FFCC33"
    ];
    function useColors() {
      if (typeof window !== "undefined" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) {
        return true;
      }
      if (typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) {
        return false;
      }
      let m;
      return typeof document !== "undefined" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance || // Is firebug? http://stackoverflow.com/a/398120/376773
      typeof window !== "undefined" && window.console && (window.console.firebug || window.console.exception && window.console.table) || // Is firefox >= v31?
      // https://developer.mozilla.org/en-US/docs/Tools/Web_Console#Styling_messages
      typeof navigator !== "undefined" && navigator.userAgent && (m = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(m[1], 10) >= 31 || // Double check webkit in userAgent just in case we are in a worker
      typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/);
    }
    function formatArgs(args) {
      args[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + args[0] + (this.useColors ? "%c " : " ") + "+" + module2.exports.humanize(this.diff);
      if (!this.useColors) {
        return;
      }
      const c = "color: " + this.color;
      args.splice(1, 0, c, "color: inherit");
      let index = 0;
      let lastC = 0;
      args[0].replace(/%[a-zA-Z%]/g, (match) => {
        if (match === "%%") {
          return;
        }
        index++;
        if (match === "%c") {
          lastC = index;
        }
      });
      args.splice(lastC, 0, c);
    }
    exports2.log = console.debug || console.log || (() => {
    });
    function save(namespaces) {
      try {
        if (namespaces) {
          exports2.storage.setItem("debug", namespaces);
        } else {
          exports2.storage.removeItem("debug");
        }
      } catch (error) {
      }
    }
    function load() {
      let r;
      try {
        r = exports2.storage.getItem("debug");
      } catch (error) {
      }
      if (!r && typeof process !== "undefined" && "env" in process) {
        r = process.env.DEBUG;
      }
      return r;
    }
    function localstorage() {
      try {
        return localStorage;
      } catch (error) {
      }
    }
    module2.exports = require_common()(exports2);
    var { formatters } = module2.exports;
    formatters.j = function(v) {
      try {
        return JSON.stringify(v);
      } catch (error) {
        return "[UnexpectedJSONParseError]: " + error.message;
      }
    };
  }
});

// ../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/node.js
var require_node = __commonJS({
  "../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/node.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var tty = require("tty");
    var util = require("util");
    exports2.init = init;
    exports2.log = log7;
    exports2.formatArgs = formatArgs;
    exports2.save = save;
    exports2.load = load;
    exports2.useColors = useColors;
    exports2.destroy = util.deprecate(
      () => {
      },
      "Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`."
    );
    exports2.colors = [6, 2, 3, 4, 5, 1];
    try {
      const supportsColor = require("supports-color");
      if (supportsColor && (supportsColor.stderr || supportsColor).level >= 2) {
        exports2.colors = [
          20,
          21,
          26,
          27,
          32,
          33,
          38,
          39,
          40,
          41,
          42,
          43,
          44,
          45,
          56,
          57,
          62,
          63,
          68,
          69,
          74,
          75,
          76,
          77,
          78,
          79,
          80,
          81,
          92,
          93,
          98,
          99,
          112,
          113,
          128,
          129,
          134,
          135,
          148,
          149,
          160,
          161,
          162,
          163,
          164,
          165,
          166,
          167,
          168,
          169,
          170,
          171,
          172,
          173,
          178,
          179,
          184,
          185,
          196,
          197,
          198,
          199,
          200,
          201,
          202,
          203,
          204,
          205,
          206,
          207,
          208,
          209,
          214,
          215,
          220,
          221
        ];
      }
    } catch (error) {
    }
    exports2.inspectOpts = Object.keys(process.env).filter((key) => {
      return /^debug_/i.test(key);
    }).reduce((obj, key) => {
      const prop = key.substring(6).toLowerCase().replace(/_([a-z])/g, (_, k) => {
        return k.toUpperCase();
      });
      let val = process.env[key];
      if (/^(yes|on|true|enabled)$/i.test(val)) {
        val = true;
      } else if (/^(no|off|false|disabled)$/i.test(val)) {
        val = false;
      } else if (val === "null") {
        val = null;
      } else {
        val = Number(val);
      }
      obj[prop] = val;
      return obj;
    }, {});
    function useColors() {
      return "colors" in exports2.inspectOpts ? Boolean(exports2.inspectOpts.colors) : tty.isatty(process.stderr.fd);
    }
    function formatArgs(args) {
      const { namespace: name, useColors: useColors2 } = this;
      if (useColors2) {
        const c = this.color;
        const colorCode = "\x1B[3" + (c < 8 ? c : "8;5;" + c);
        const prefix = `  ${colorCode};1m${name} \x1B[0m`;
        args[0] = prefix + args[0].split("\n").join("\n" + prefix);
        args.push(colorCode + "m+" + module2.exports.humanize(this.diff) + "\x1B[0m");
      } else {
        args[0] = getDate() + name + " " + args[0];
      }
    }
    function getDate() {
      if (exports2.inspectOpts.hideDate) {
        return "";
      }
      return (/* @__PURE__ */ new Date()).toISOString() + " ";
    }
    function log7(...args) {
      return process.stderr.write(util.formatWithOptions(exports2.inspectOpts, ...args) + "\n");
    }
    function save(namespaces) {
      if (namespaces) {
        process.env.DEBUG = namespaces;
      } else {
        delete process.env.DEBUG;
      }
    }
    function load() {
      return process.env.DEBUG;
    }
    function init(debug5) {
      debug5.inspectOpts = {};
      const keys = Object.keys(exports2.inspectOpts);
      for (let i = 0; i < keys.length; i++) {
        debug5.inspectOpts[keys[i]] = exports2.inspectOpts[keys[i]];
      }
    }
    module2.exports = require_common()(exports2);
    var { formatters } = module2.exports;
    formatters.o = function(v) {
      this.inspectOpts.colors = this.useColors;
      return util.inspect(v, this.inspectOpts).split("\n").map((str) => str.trim()).join(" ");
    };
    formatters.O = function(v) {
      this.inspectOpts.colors = this.useColors;
      return util.inspect(v, this.inspectOpts);
    };
  }
});

// ../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/index.js
var require_src = __commonJS({
  "../../../.yarn/__virtual__/debug-virtual-4a69f763f8/0/cache/debug-npm-4.4.0-f1ca372596-db94f1a182.zip/node_modules/debug/src/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    if (typeof process === "undefined" || process.type === "renderer" || process.browser === true || process.__nwjs) {
      module2.exports = require_browser();
    } else {
      module2.exports = require_node();
    }
  }
});

// src/index.ts
var src_exports = {};
__export(src_exports, {
  babelBuild: () => import_web_config3.babelBuild,
  babelJsonReplacementPlugin: () => import_web_config3.babelJsonReplacementPlugin,
  getPackageManager: () => import_web_config3.getPackageManager,
  initialize: () => initialize,
  loadConfig: () => import_web_config3.loadConfig
});
module.exports = __toCommonJS(src_exports);
init_cjs_shims();
var import_clipanion7 = require("clipanion");

// src/BuildCommand.ts
init_cjs_shims();
var import_path7 = __toESM(require("path"), 1);
var p3 = __toESM(require("@clack/prompts"), 1);
var import_utils4 = require("@granite-js/utils");
var import_clipanion = require("clipanion");

// src/build/buildArtifact/index.ts
init_cjs_shims();
var p2 = __toESM(require("@clack/prompts"), 1);

// src/build/buildArtifact/defaultBuildDeps.ts
init_cjs_shims();
var import_plugins = require("@apps-in-toss/plugins");
var import_uuidv7 = require("uuidv7");

// src/build/buildArtifact/utils.ts
init_cjs_shims();
var import_promises = __toESM(require("fs/promises"), 1);
var import_path = __toESM(require("path"), 1);

// src/build/buildArtifact/constants.ts
init_cjs_shims();
var RUNTIME_BUILD_DEFINITIONS = [
  { runtimeVersion: "0.84.0" },
  { runtimeVersion: "0.72.6", configTargetVersion: "0.72.6" }
];
var DIST_DIRECTORY = "dist";

// src/build/buildArtifact/utils.ts
async function clearBuildCache(packageRoot) {
  await import_promises.default.rm(import_path.default.join(packageRoot, DIST_DIRECTORY), { recursive: true, force: true });
}
async function ensureGraniteConfigExists(graniteConfigPath) {
  try {
    await import_promises.default.access(graniteConfigPath);
  } catch {
    throw new Error(`Cannot find granite config: ${graniteConfigPath}`);
  }
}
async function resolveArtifactOptions(strategy, context) {
  if (strategy.getArtifactOptions == null) {
    return { isGame: context.appType === "game" };
  }
  return strategy.getArtifactOptions(context);
}
async function readAppManifest(appJsonPath) {
  const appJsonRaw = await import_promises.default.readFile(appJsonPath, "utf8");
  const appJson = JSON.parse(appJsonRaw);
  if (typeof appJson.appName !== "string" || appJson.appName.length === 0) {
    throw new Error(`Invalid appName in ${appJsonPath}`);
  }
  return {
    appName: appJson.appName,
    appType: appJson.appType ?? void 0
  };
}

// src/build/resolveBuildStrategy.ts
init_cjs_shims();
var import_promises2 = __toESM(require("fs/promises"), 1);
async function resolveBuildStrategy(graniteConfigPath, strategyFactories) {
  const graniteConfigSource = await import_promises2.default.readFile(graniteConfigPath, "utf8");
  for (const strategy of strategyFactories) {
    if (await strategy.match({ graniteConfigPath, graniteConfigSource })) {
      return strategy;
    }
  }
  throw new Error(`Cannot resolve build strategy: ${graniteConfigPath}`);
}

// src/build/buildArtifact/defaultBuildDeps.ts
var defaultBuildDeps = {
  clearBuildCache,
  resolveStrategy: resolveBuildStrategy,
  collectDependencyVersions: import_plugins.collectDependencyVersions,
  createArtifact: import_plugins.createArtifact,
  getSdkVersion: import_plugins.getSdkVersion,
  generateDeploymentId: import_uuidv7.uuidv7
};

// src/build/buildArtifact/prepareArtifact.ts
init_cjs_shims();
var import_path2 = __toESM(require("path"), 1);

// src/utils/constants.ts
init_cjs_shims();
var APP_MANIFEST_NAME = "app.json";
var DEFAULT_API_BASE_URL = "https://apps-in-toss.toss.im/console";

// src/build/buildArtifact/prepareArtifact.ts
async function prepareArtifact({
  packageRoot,
  preparation,
  services
}) {
  const appJsonPath = import_path2.default.join(packageRoot, ".granite", APP_MANIFEST_NAME);
  const { appName, appType } = await readAppManifest(appJsonPath);
  const outfile = import_path2.default.join(packageRoot, `${appName}.ait`);
  const { dependencies, devDependencies } = await services.collectDependencyVersions(packageRoot);
  const sdkVersion = services.getSdkVersion();
  const artifactOptions = await resolveArtifactOptions(preparation.strategy, {
    ...preparation.context,
    appName,
    appType
  });
  return {
    appJsonPath,
    appName,
    appType,
    outfile,
    sdkVersion,
    dependencies,
    devDependencies,
    artifactOptions
  };
}

// src/build/buildArtifact/prepareBuild.ts
init_cjs_shims();
var import_path3 = __toESM(require("path"), 1);
async function prepareBuild({
  packageRoot,
  services,
  strategies
}) {
  const graniteConfigPath = import_path3.default.join(packageRoot, "granite.config.ts");
  const deploymentId = services.generateDeploymentId();
  await ensureGraniteConfigExists(graniteConfigPath);
  await services.clearBuildCache(packageRoot);
  const strategy = await services.resolveStrategy(graniteConfigPath, strategies);
  const buildInput = {
    runtimeBuildDefinitions: RUNTIME_BUILD_DEFINITIONS
  };
  if (buildInput.runtimeBuildDefinitions.length === 0) {
    throw new Error("No runtime build definitions provided");
  }
  return {
    strategy,
    context: { packageRoot, deploymentId, graniteConfigPath },
    buildInput,
    primaryRuntimeVersion: buildInput.runtimeBuildDefinitions[0].runtimeVersion
  };
}

// src/build/buildArtifact/runStrategyBuild.ts
init_cjs_shims();
var p = __toESM(require("@clack/prompts"), 1);
async function runStrategyBuild(preparation) {
  const buildResults = await preparation.strategy.build(preparation.buildInput, preparation.context);
  const bundleFiles = [];
  for (let i = 0; i < buildResults.length; i++) {
    const result = buildResults[i];
    bundleFiles.push(...result.bundleFiles);
    p.log.step(`[${i + 1}/${buildResults.length}] Built for RN ${result.runtimeVersion}`);
  }
  return bundleFiles;
}

// src/build/RNBuildStrategy.ts
init_cjs_shims();
var import_promises4 = __toESM(require("fs/promises"), 1);
var import_path5 = __toESM(require("path"), 1);
var import_web_config = require("@apps-in-toss/web-config");
var import_execa = require("execa");

// src/build/createRuntimeBundleFiles.ts
init_cjs_shims();
var import_promises3 = __toESM(require("fs/promises"), 1);
var import_path4 = __toESM(require("path"), 1);
async function createRuntimeBundleFiles({
  packageRoot,
  runtimeVersion,
  bundleOutputDir
}) {
  const distPath = import_path4.default.isAbsolute(bundleOutputDir) ? bundleOutputDir : import_path4.default.join(packageRoot, bundleOutputDir);
  const platforms = ["ios", "android"];
  const runtimeName = runtimeVersion.replace(/\./g, "_");
  const bundleFiles = [];
  for (const platform of platforms) {
    const sourceJsPath = import_path4.default.join(distPath, `bundle.${platform}.js`);
    const sourceMapPath = import_path4.default.join(distPath, `bundle.${platform}.js.map`);
    const targetJsPath = import_path4.default.join(distPath, `bundle.${platform}.${runtimeName}.js`);
    const targetMapPath = import_path4.default.join(distPath, `bundle.${platform}.${runtimeName}.js.map`);
    await copyRequiredFile(sourceJsPath, targetJsPath);
    await copyRequiredFile(sourceMapPath, targetMapPath);
    bundleFiles.push({
      path: targetJsPath,
      platform,
      runtimeVersion
    });
    bundleFiles.push({
      path: targetMapPath,
      platform,
      runtimeVersion
    });
  }
  return bundleFiles;
}
async function copyRequiredFile(sourcePath, targetPath) {
  try {
    await import_promises3.default.copyFile(sourcePath, targetPath);
  } catch {
    throw new Error(`Missing build output file: ${sourcePath}`);
  }
}

// src/utils/injectAppsInTossTarget.ts
init_cjs_shims();

// src/MigrateCommand/codemod/index.ts
init_cjs_shims();

// src/MigrateCommand/codemod/transform.ts
init_cjs_shims();
var import_jscodeshift = __toESM(require("jscodeshift"), 1);
function transform(source, { transformers: transformers2, options = {} }) {
  const j = import_jscodeshift.default.withParser("tsx");
  const report = [];
  const stats = /* @__PURE__ */ new Map();
  const context = {
    j,
    jscodeshift: j,
    stats: (name, quantity = 1) => {
      if (quantity === 0) {
        return;
      }
      stats.set(name, (stats.get(name) ?? 0) + quantity);
    },
    report: (message) => {
      report.push(message);
    }
  };
  const root = transformers2.reduce((root2, transformer) => transformer(root2, context, options), j(source));
  return root.toSource();
}

// src/utils/injectAppsInTossTarget.ts
var TARGET_IMPORTS = [
  { source: "@apps-in-toss/framework/plugins", importName: "appsInToss" },
  { source: "@apps-in-toss/plugins", importName: "appsInToss" },
  { source: "@apps-in-toss/framework/internal", importName: "appsInTossHost" },
  { source: "@apps-in-toss/plugins/internal", importName: "appsInTossHost" }
];
function createAppsInTossTargetTransformer(stats) {
  return (root, { j }, options) => {
    const fileName = options?.fileName ?? "<unknown>";
    const targetVersion = options?.targetVersion;
    if (typeof targetVersion !== "string" || targetVersion.length === 0) {
      return root;
    }
    const localNames = /* @__PURE__ */ new Set();
    let warnedForUnsupportedArg = false;
    for (const targetImport of TARGET_IMPORTS) {
      root.find(j.ImportDeclaration, {
        source: { value: targetImport.source }
      }).forEach((importPath) => {
        for (const specifier of importPath.node.specifiers ?? []) {
          if (specifier.type === "ImportSpecifier" && specifier.imported.type === "Identifier" && specifier.imported.name === targetImport.importName) {
            localNames.add(specifier.local?.name ?? targetImport.importName);
          }
        }
      });
    }
    if (localNames.size === 0) {
      return root;
    }
    root.find(j.CallExpression).forEach((path13) => {
      const callee = path13.node.callee;
      if (!j.Identifier.check(callee) || !localNames.has(callee.name)) {
        return;
      }
      stats.matchedCallCount += 1;
      const firstArg = path13.node.arguments[0];
      if (firstArg == null) {
        path13.node.arguments = [
          j.objectExpression([j.objectProperty(j.identifier("target"), j.stringLiteral(targetVersion))])
        ];
        stats.updatedCallCount += 1;
        return;
      }
      if (!j.ObjectExpression.check(firstArg)) {
        stats.hadUnsupportedArgument = true;
        if (!warnedForUnsupportedArg) {
          warnedForUnsupportedArg = true;
          options?.onUnsupportedArgument?.(fileName);
        }
        return;
      }
      const targetProp = firstArg.properties.find((property) => {
        if (!j.ObjectProperty.check(property)) {
          return false;
        }
        if (j.Identifier.check(property.key)) {
          return property.key.name === "target";
        }
        return j.StringLiteral.check(property.key) && property.key.value === "target";
      });
      if (targetProp != null && j.ObjectProperty.check(targetProp)) {
        targetProp.value = j.stringLiteral(targetVersion);
        stats.updatedCallCount += 1;
        return;
      }
      firstArg.properties.unshift(j.objectProperty(j.identifier("target"), j.stringLiteral(targetVersion)));
      stats.updatedCallCount += 1;
    });
    return root;
  };
}
function injectAppsInTossTargetWithResult(source, options) {
  const stats = {
    matchedCallCount: 0,
    updatedCallCount: 0,
    hadUnsupportedArgument: false
  };
  const code = transform(source, {
    transformers: [createAppsInTossTargetTransformer(stats)],
    options
  });
  return {
    code,
    ...stats
  };
}

// src/build/RNBuildStrategy.ts
var GRANITE_DIRECTORY = ".granite";
var TEMP_CONFIG_FILE_PREFIX = ".ait-runtime";
var BUNDLE_OUTPUT_DIRECTORY = "dist";
var RNBuildStrategy = class {
  name = "rn";
  match(context) {
    return true;
  }
  async build(input, context) {
    const results = [];
    const graniteCommand = resolveGraniteCommand();
    for (const definition of input.runtimeBuildDefinitions) {
      const graniteBuildArgs = await resolveGraniteBuildArgs(definition, context);
      const tempConfigPath = graniteBuildArgs.tempConfigPath;
      const commandArgs = [...graniteCommand.prefixArgs, "build", ...graniteBuildArgs.args];
      try {
        await (0, import_execa.execa)(graniteCommand.executable, commandArgs, {
          cwd: context.packageRoot,
          env: {
            ...process.env,
            AIT_DEPLOYMENT_ID: context.deploymentId
          },
          stdio: "inherit"
        });
      } catch (error) {
        throw new Error(`granite build failed: ${graniteCommand.executable} ${commandArgs.join(" ")}`, {
          cause: error
        });
      } finally {
        if (tempConfigPath != null) {
          await import_promises4.default.rm(tempConfigPath, { force: true });
        }
      }
      results.push({
        runtimeVersion: definition.runtimeVersion,
        bundleFiles: await createRuntimeBundleFiles({
          packageRoot: context.packageRoot,
          runtimeVersion: definition.runtimeVersion,
          bundleOutputDir: BUNDLE_OUTPUT_DIRECTORY
        })
      });
    }
    return results;
  }
  async getArtifactOptions(context) {
    return {
      isGame: context.appType === "game"
    };
  }
};
function resolveGraniteCommand() {
  const { packageManager } = (0, import_web_config.getPackageManager)({ isExecutor: true });
  return {
    executable: packageManager,
    prefixArgs: ["granite"]
  };
}
async function resolveGraniteBuildArgs(definition, context) {
  if (definition.configTargetVersion == null) {
    return { args: ["--no-cache"] };
  }
  const targetVersion = definition.configTargetVersion;
  const tempConfigRelativePath = createTempConfigRelativePath(targetVersion);
  const tempConfigPath = import_path5.default.join(context.packageRoot, tempConfigRelativePath);
  try {
    await writeRuntimeConfig(context.graniteConfigPath, tempConfigPath, targetVersion);
    return {
      args: ["--config", tempConfigRelativePath, "--no-cache"],
      tempConfigPath
    };
  } catch {
    return { args: ["--no-cache"], tempConfigPath };
  }
}
function createTempConfigRelativePath(targetVersion) {
  const runtimeName = targetVersion.replace(/\./g, "_");
  return import_path5.default.join(GRANITE_DIRECTORY, `${TEMP_CONFIG_FILE_PREFIX}-${runtimeName}.config.ts`);
}
async function writeRuntimeConfig(graniteConfigPath, tempConfigPath, targetVersion) {
  const source = await import_promises4.default.readFile(graniteConfigPath, "utf8");
  const transformed = injectAppsInTossTargetWithResult(source, {
    fileName: graniteConfigPath,
    targetVersion
  });
  if (transformed.matchedCallCount === 0) {
    throw new Error(
      `Cannot inject target (${targetVersion}) into ${graniteConfigPath}: appsInToss/appsInTossHost call not found`
    );
  }
  if (transformed.hadUnsupportedArgument) {
    throw new Error(
      `Cannot inject target (${targetVersion}) into ${graniteConfigPath}: unsupported plugin argument shape detected`
    );
  }
  await import_promises4.default.mkdir(import_path5.default.dirname(tempConfigPath), { recursive: true });
  await import_promises4.default.writeFile(tempConfigPath, transformed.code, "utf8");
}

// src/build/WebBuildStrategy.ts
init_cjs_shims();
var import_promises5 = __toESM(require("fs/promises"), 1);
var import_module = require("module");
var import_path6 = __toESM(require("path"), 1);
var import_web_config2 = require("@apps-in-toss/web-config");
var import_execa2 = require("execa");
var APP_MANIFEST_NAME2 = "app.json";
var WebBuildStrategy = class {
  name = "web";
  config;
  prepared = false;
  match({ graniteConfigSource }) {
    return graniteConfigSource.includes("@apps-in-toss/web-framework");
  }
  async build(input, context) {
    const config = await this.ensurePrepared(context);
    const results = [];
    for (const definition of input.runtimeBuildDefinitions) {
      for (const platform of ["ios", "android"]) {
        const buildResult = await buildRuntimeBundle({
          config,
          runtimeVersion: definition.runtimeVersion,
          deploymentId: context.deploymentId,
          packageRoot: context.packageRoot,
          platform
        });
        await (0, import_web_config2.writeBundle)(buildResult, { platform, config, cwd: context.packageRoot });
      }
      results.push({
        runtimeVersion: definition.runtimeVersion,
        bundleFiles: await createRuntimeBundleFiles({
          packageRoot: context.packageRoot,
          runtimeVersion: definition.runtimeVersion,
          bundleOutputDir: config.outdir
        })
      });
    }
    return results;
  }
  async getArtifactOptions(context) {
    const config = await this.ensureConfig();
    const isGame = config.webViewProps?.type === "game";
    const extra = {
      brand: config.brand,
      ...config.navigationBar != null ? { navigationBar: config.navigationBar } : {},
      ...config.webViewProps != null ? { webViewProps: config.webViewProps } : {}
    };
    const webDistDir = import_path6.default.join(context.packageRoot, config.outdir, "web");
    const additionalFilesToZip = await getWebDistFiles(webDistDir);
    return { isGame, additionalFilesToZip, extra };
  }
  async ensurePrepared(context) {
    const config = await this.ensureConfig();
    if (this.prepared) {
      return config;
    }
    const outdir = import_path6.default.join(context.packageRoot, config.outdir);
    const webDistDir = import_path6.default.join(context.packageRoot, config.outdir, "web");
    try {
      const files = await import_promises5.default.readdir(webDistDir);
      if (files.includes("index.html")) {
        await this.writeAppJson(context);
        this.prepared = true;
        return config;
      }
    } catch {
    }
    const { packageManager } = (0, import_web_config2.getPackageManager)({ isExecutor: true });
    await import_promises5.default.rm(outdir, { recursive: true, force: true });
    await (0, import_execa2.execa)(packageManager, config.web.commands.build.split(" "), {
      cwd: context.packageRoot,
      stdio: "inherit",
      shell: true
    });
    await import_promises5.default.mkdir(webDistDir, { recursive: true });
    const items = await import_promises5.default.readdir(outdir);
    for (const item of items) {
      const src = import_path6.default.join(outdir, item);
      const dest = import_path6.default.join(webDistDir, item);
      if (src === webDistDir) {
        continue;
      }
      await import_promises5.default.rename(src, dest);
    }
    const webDistFiles = await import_promises5.default.readdir(webDistDir);
    if (webDistFiles.length === 0) {
      throw new Error(
        `Web build output is empty. Check that your outdir path and web.commands.build output path match in granite.config.ts.`
      );
    }
    if (!webDistFiles.includes("index.html")) {
      throw new Error(
        `Web build output requires ${config.outdir}/web/index.html. Currently only CSR / SSG environments are supported.`
      );
    }
    await this.writeAppJson(context);
    this.prepared = true;
    return config;
  }
  async writeAppJson(context) {
    const config = await this.ensureConfig();
    const graniteDir = import_path6.default.join(context.packageRoot, ".granite");
    await import_promises5.default.mkdir(graniteDir, { recursive: true });
    await import_promises5.default.writeFile(
      import_path6.default.join(graniteDir, APP_MANIFEST_NAME2),
      JSON.stringify(
        {
          appName: config.appName,
          permissions: config.permissions
        },
        null,
        2
      ),
      "utf8"
    );
  }
  async ensureConfig() {
    if (this.config) {
      return this.config;
    }
    const config = await (0, import_web_config2.loadConfig)();
    if (!config) {
      throw new Error("Cannot find granite config");
    }
    this.config = config;
    return this.config;
  }
};
async function buildRuntimeBundle({
  config,
  runtimeVersion,
  deploymentId,
  packageRoot,
  platform = "ios"
}) {
  const buildNumber = getBuildNumber();
  const projectRequire = (0, import_module.createRequire)(import_path6.default.join(packageRoot, "package.json"));
  const rnSuffix = runtimeVersion.startsWith("0.72") ? "rn72" : "rn84";
  const prebuiltPath = projectRequire.resolve(`@apps-in-toss/web-framework/prebuilt/prod/${platform}/${rnSuffix}`);
  return (0, import_web_config2.babelBuild)({
    config,
    platform,
    buildNumber,
    deploymentId,
    prebuiltPath
  });
}
function getBuildNumber() {
  const date = /* @__PURE__ */ new Date();
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, "0");
  const day = String(date.getDate()).padStart(2, "0");
  const hours = String(date.getHours()).padStart(2, "0");
  const minutes = String(date.getMinutes()).padStart(2, "0");
  const seconds = String(date.getSeconds()).padStart(2, "0");
  return `${year}${month}${day}${hours}${minutes}${seconds}`;
}
async function getWebDistFiles(webDistDir) {
  const additionalFilesToZip = [];
  try {
    const allFiles = await import_promises5.default.readdir(webDistDir, { recursive: true });
    const webFiles = await Promise.all(
      allFiles.map(async (file) => {
        const filePath = import_path6.default.join(webDistDir, String(file));
        const stat = await import_promises5.default.stat(filePath);
        return stat.isFile() ? String(file) : null;
      })
    ).then((files) => files.filter((file) => file !== null));
    additionalFilesToZip.push(
      ...webFiles.map((file) => ({
        path: import_path6.default.join(webDistDir, file),
        name: import_path6.default.join("web", file)
      }))
    );
  } catch {
  }
  return additionalFilesToZip;
}

// src/build/buildArtifact/index.ts
async function buildArtifact(packageRoot, deps = {}) {
  const services = {
    ...defaultBuildDeps,
    ...deps
  };
  const strategies = [new WebBuildStrategy(), new RNBuildStrategy()];
  const preparation = await prepareBuild({ packageRoot, services, strategies });
  const bundleFiles = await runStrategyBuild(preparation);
  const artifact = await prepareArtifact({ packageRoot, preparation, services });
  p2.log.step("Creating .ait artifact...");
  await services.createArtifact({
    reactNativeVersion: preparation.primaryRuntimeVersion,
    sdkVersion: artifact.sdkVersion,
    isGame: artifact.artifactOptions.isGame,
    isWeb: preparation.strategy.name === "web",
    appJsonPath: artifact.appJsonPath,
    deploymentId: preparation.context.deploymentId,
    packageJson: {
      dependencies: artifact.dependencies,
      devDependencies: artifact.devDependencies
    },
    bundleFiles,
    outfile: artifact.outfile,
    ...artifact.artifactOptions.additionalFilesToZip != null ? { additionalFilesToZip: artifact.artifactOptions.additionalFilesToZip } : {},
    ...artifact.artifactOptions.extra != null ? { extra: artifact.artifactOptions.extra } : {}
  });
  return { outfile: artifact.outfile, deploymentId: preparation.context.deploymentId };
}

// src/BuildCommand.ts
var BuildCommand = class extends import_clipanion.Command {
  static paths = [["build"]];
  static usage = import_clipanion.Command.Usage({
    category: "Build",
    description: "\uC571\uC778\uD1A0\uC2A4 \uBC30\uD3EC\uC6A9 .ait \uC544\uD2F0\uD329\uD2B8\uB97C \uBE4C\uB4DC\uD569\uB2C8\uB2E4.",
    examples: [["Build artifact", "ait build"]]
  });
  async execute() {
    const packageRoot = (0, import_utils4.getPackageRoot)();
    const buildResult = await buildArtifact(packageRoot);
    p3.log.success(`AIT build completed (${import_path7.default.basename(buildResult.outfile)})`);
    p3.log.info(`deploymentId: ${buildResult.deploymentId}`);
  }
};

// src/DeployCommand.ts
init_cjs_shims();
var import_assert = __toESM(require("assert"), 1);
var import_fs3 = __toESM(require("fs"), 1);
var import_path9 = __toESM(require("path"), 1);
var import_ait_format = require("@apps-in-toss/ait-format");
var p4 = __toESM(require("@clack/prompts"), 1);
var import_utils5 = require("@granite-js/utils");
var import_clipanion2 = require("clipanion");
var import_picocolors = __toESM(require_picocolors(), 1);

// src/utils/TokenStorage.ts
init_cjs_shims();
var import_fs = __toESM(require("fs"), 1);
var import_os = __toESM(require("os"), 1);
var import_path8 = __toESM(require("path"), 1);
var TokenStorage = class {
  static get path() {
    const home = import_os.default.homedir();
    const dir = import_path8.default.join(home, ".ait");
    if (!import_fs.default.existsSync(dir)) {
      import_fs.default.mkdirSync(dir, { recursive: true });
    }
    return import_path8.default.join(dir, "credentials");
  }
  static read() {
    const file = this.path;
    if (!import_fs.default.existsSync(file)) {
      return {};
    }
    const raw = import_fs.default.readFileSync(file, "utf8");
    try {
      const parsed = JSON.parse(raw);
      if (parsed && typeof parsed === "object") {
        return parsed;
      }
    } catch {
    }
    return {};
  }
  static write(map) {
    const file = this.path;
    const content = JSON.stringify(map, null, 2);
    import_fs.default.writeFileSync(file, content, { encoding: "utf8" });
  }
  static set(workspace, token) {
    const creds = this.read();
    creds[workspace] = token;
    this.write(creds);
  }
  static delete(workspace) {
    const creds = this.read();
    if (workspace in creds) {
      delete creds[workspace];
      this.write(creds);
      return true;
    }
    return false;
  }
  static get(workspace) {
    const creds = this.read();
    return creds[workspace];
  }
};

// src/utils/upload.ts
init_cjs_shims();
var import_fs2 = __toESM(require("fs"), 1);
var import_debug2 = __toESM(require_src(), 1);

// src/utils/flowAsync.ts
init_cjs_shims();
function flowAsync(...funcs) {
  return async function(...args) {
    let result = funcs.length ? await funcs[0].apply(this, args) : args[0];
    for (let i = 1; i < funcs.length; i++) {
      result = await funcs[i]?.call(this, result);
    }
    return result;
  };
}

// src/utils/handleFetchResponse.ts
init_cjs_shims();
var import_debug = __toESM(require_src(), 1);
var debug = (0, import_debug.default)("api");
async function handleFetchResponse(response) {
  debug(`Response ${response.status} ${response.statusText}`);
  if (!response.ok) {
    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
  }
  const data = await response.clone().json();
  debug("response", data);
  if (data.resultType !== "SUCCESS") {
    const errorCode = data?.error?.errorCode ?? "-1";
    const errorReason = data?.error?.reason ?? "unknown";
    throw new Error(`${errorReason} (Code: ${errorCode})`);
  }
  return data.success;
}

// src/utils/withRetry.ts
init_cjs_shims();
var import_promises6 = require("timers/promises");
async function withRetry(fn, options = {}) {
  const { maxRetries = 60, delayMs = 1e4, shouldRetryOnResult, shouldRetryOnError, onRetry } = options;
  let attempt = 0;
  while (attempt < maxRetries) {
    try {
      const result = await fn();
      if (shouldRetryOnResult && shouldRetryOnResult(result)) {
        onRetry?.(attempt + 1, { result });
        await (0, import_promises6.setTimeout)(delayMs);
        attempt++;
        continue;
      }
      return result;
    } catch (error) {
      const retryable = shouldRetryOnError ? shouldRetryOnError(error) : false;
      if (!retryable) {
        throw error;
      }
      onRetry?.(attempt + 1, { error });
      await (0, import_promises6.setTimeout)(delayMs);
      attempt++;
    }
  }
  throw new Error("\uCD5C\uB300 \uB300\uAE30\uC2DC\uAC04\uC744 \uCD08\uACFC\uD588\uC5B4\uC694.");
}

// src/utils/upload.ts
var debug2 = (0, import_debug2.default)("cli:deploy");
async function uploadArtifact(config) {
  debug2("uploadArtifact", config);
  return flowAsync(uploadStart(), uploadArtifactToRemote(), uploadComplete(), checkBundleStatus())(config);
}
function uploadStart() {
  return async (config) => {
    debug2("uploadStart", config);
    const baseUrl = config.baseUrl ?? DEFAULT_API_BASE_URL;
    const response = await fetch(`${baseUrl}/api-public/v3/openapi/bundles/${config.appName}/upload-start`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${config.apiKey}`
      },
      body: JSON.stringify({
        deploymentId: config.deploymentId,
        memo: config.memo
      })
    });
    return {
      config,
      output: await handleFetchResponse(response)
    };
  };
}
function uploadArtifactToRemote() {
  return async (params) => {
    const { config, output } = params;
    debug2("uploadArtifactToRemote", config, output);
    const stat = await import_fs2.default.promises.stat(config.artifactPath);
    const stream = import_fs2.default.createReadStream(config.artifactPath);
    const response = await fetch(output.uploadUrl, {
      method: "PUT",
      headers: {
        "Content-Type": "application/zip",
        "Content-Length": String(stat.size)
      },
      body: stream,
      duplex: "half"
    });
    if (!response.ok) {
      throw new Error("\uC5C5\uB85C\uB4DC\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4. \uB2E4\uC2DC \uC2DC\uB3C4\uD574\uC8FC\uC138\uC694.");
    }
    return config;
  };
}
function uploadComplete() {
  return async (config) => {
    debug2("uploadComplete", config);
    const baseUrl = config.baseUrl ?? DEFAULT_API_BASE_URL;
    const response = await fetch(`${baseUrl}/api-public/v3/openapi/bundles/${config.appName}/upload-complete`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${config.apiKey}`
      },
      body: JSON.stringify({
        deploymentId: config.deploymentId
      })
    });
    await handleFetchResponse(response);
    return config;
  };
}
function checkBundleStatus(delayMs = 1e4, maxRetries = 10) {
  return async (config) => {
    const baseUrl = config.baseUrl ?? DEFAULT_API_BASE_URL;
    function assertBuildNotFailed(config2, reviewStatus2) {
      if (reviewStatus2 === "BUILD_FAILED") {
        throw new Error(
          `\uBE4C\uB4DC\uC5D0 \uC2E4\uD328\uD588\uC2B5\uB2C8\uB2E4. \uCF58\uC194\uC5D0\uC11C \uBE4C\uB4DC \uC2E4\uD328 \uC0AC\uC720\uB97C \uD655\uC778\uD574\uC8FC\uC138\uC694.(deploymentId: ${config2.deploymentId})`
        );
      }
    }
    async function pollBundleStatus() {
      const response = await fetch(
        `${baseUrl}/api-public/v3/openapi/bundles/${config.appName}/deployments/${config.deploymentId}`,
        {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${config.apiKey}`
          }
        }
      );
      const { reviewStatus: reviewStatus2 } = await handleFetchResponse(response);
      return reviewStatus2;
    }
    const reviewStatus = await withRetry(pollBundleStatus, {
      maxRetries,
      delayMs,
      shouldRetryOnResult: (reviewStatus2) => reviewStatus2 === "BUILDING" || reviewStatus2 === "PREPARE",
      shouldRetryOnError: () => false,
      onRetry: (attempt, info) => debug2("checkBundleStatus: retry", { attempt, ...info })
    });
    assertBuildNotFailed(config, reviewStatus);
  };
}

// src/DeployCommand.ts
var DeployCommand = class extends import_clipanion2.Command {
  static paths = [["deploy"]];
  static usage = import_clipanion2.Command.Usage({
    category: "Deploy",
    description: "\uBE4C\uB4DC\uB41C .ait \uC544\uD2F0\uD329\uD2B8\uB97C \uC571\uC778\uD1A0\uC2A4\uC5D0 \uC5C5\uB85C\uB4DC(\uBC30\uD3EC)\uD569\uB2C8\uB2E4.",
    examples: [
      ["\uAE30\uBCF8 \uC544\uD2F0\uD329\uD2B8(.ait) \uBC30\uD3EC", "ait deploy"],
      ["\uD2B9\uC815 .ait \uD30C\uC77C \uBC30\uD3EC", "ait deploy --location ./dist/my-app.ait"],
      ["\uD504\uB85C\uD544\uC5D0 \uC800\uC7A5\uB41C \uD1A0\uD070\uC73C\uB85C \uBC30\uD3EC", "ait deploy --profile dev"],
      ["\uCD9C\uC2DC \uBA54\uBAA8\uC640 \uD568\uAED8 \uBC30\uD3EC", 'ait deploy -m "\uCD9C\uC2DC \uBA54\uBAA8(\uCD5C\uB300 1000\uC790)"'],
      ["\uBC30\uD3EC \uACB0\uACFC scheme\uB9CC \uCD9C\uB825", "ait deploy --scheme-only"]
    ]
  });
  apiKey = import_clipanion2.Option.String("--api-key", {
    required: false,
    description: "\uBC30\uD3EC\uB97C \uC704\uD55C API \uD0A4\uC785\uB2C8\uB2E4. \uBA85\uC2DC\uC801\uC73C\uB85C \uC774 \uC635\uC158\uC73C\uB85C api key\uB97C \uC9C1\uC811 \uC785\uB825\uD558\uAC70\uB098, --profile \uC635\uC158\uC744 \uC0AC\uC6A9\uD574\uC11C \uD504\uB85C\uD544\uC744 \uC9C0\uC815\uD558\uBA74 \uD504\uB85C\uD544\uC5D0 \uB4F1\uB85D\uB41C api key\uB97C \uC0AC\uC6A9\uD574\uC694."
  });
  workspace = import_clipanion2.Option.String("--workspace", {
    required: false,
    description: "(deprecated) \uD1A0\uD070 \uC6CC\uD06C\uC2A4\uD398\uC774\uC2A4 \uC774\uB984\uC785\uB2C8\uB2E4. \uC774 \uC635\uC158 \uB300\uC2E0 --profile \uC635\uC158\uC744 \uC0AC\uC6A9\uD574\uC8FC\uC138\uC694."
  });
  profile = import_clipanion2.Option.String("--profile", {
    required: false,
    description: "ait token add \uBA85\uB839\uC5B4\uB97C \uD1B5\uD574 \uB4F1\uB85D\uD55C \uD504\uB85C\uD544\uC774\uC5D0\uC694. \uD504\uB85C\uD544\uC774 \uC5C6\uC73C\uBA74 default \uD504\uB85C\uD544\uC744 \uC0AC\uC6A9\uD574\uC694."
  });
  baseUrl = import_clipanion2.Option.String("--base-url", {
    description: "API Base URL",
    required: false
  });
  location = import_clipanion2.Option.String("--location", {
    description: "\uC5C5\uB85C\uB4DC\uD560 .ait \uD30C\uC77C\uC758 \uACBD\uB85C\uB97C \uBA85\uC2DC\uC801\uC73C\uB85C \uC9C0\uC815\uD574\uC694. \uAE30\uBCF8\uAC12\uC740 \uD604\uC7AC \uD328\uD0A4\uC9C0 \uB8E8\uD2B8 \uB514\uB809\uD1A0\uB9AC\uC5D0 \uC788\uB294 ait \uD30C\uC77C\uC774\uC5D0\uC694.",
    required: false
  });
  schemeOnly = import_clipanion2.Option.Boolean("--scheme-only", {
    required: false,
    description: "\uBC30\uD3EC \uACB0\uACFC\uB97C intoss-private scheme\uB9CC \uCD9C\uB825\uD558\uB3C4\uB85D \uC124\uC815\uD574\uC694. \uAE30\uBCF8\uAC12\uC740 false\uC608\uC694."
  });
  memo = import_clipanion2.Option.String("-m,--memo", {
    required: false,
    description: "\uCD9C\uC2DC \uBA54\uBAA8\uB97C \uC785\uB825\uD574\uC694. \uCD5C\uB300 1000\uC790\uAE4C\uC9C0 \uC785\uB825\uD560 \uC218 \uC788\uC5B4\uC694."
  });
  async execute() {
    const baseUrl = this.baseUrl;
    const profile = this.profile || this.workspace || "default";
    if (this.workspace) {
      p4.log.warn("(deprecated) --workspace \uC635\uC158\uC740 \uC774\uC81C \uC0AC\uC6A9\uD558\uC9C0 \uC54A\uC544\uC694. \uC774 \uC635\uC158 \uB300\uC2E0 --profile \uC635\uC158\uC744 \uC0AC\uC6A9\uD574\uC8FC\uC138\uC694.");
    }
    const apiKey = await this.getApiKey(profile);
    if (p4.isCancel(apiKey)) {
      return;
    }
    try {
      if (this.memo != null && this.memo.length > 1e3) {
        throw new Error("memo\uB294 \uCD5C\uB300 1000\uC790\uAE4C\uC9C0 \uC785\uB825\uD560 \uC218 \uC788\uC5B4\uC694.");
      }
      const resolvedArtifactPath = this.getArtifactPath(this.location);
      const buffer = import_fs3.default.readFileSync(resolvedArtifactPath);
      const format = import_ait_format.AppsInTossBundle.detect(buffer);
      const { appName, deploymentId } = await (async () => {
        if (format === import_ait_format.AppsInTossBundle.Format.AIT) {
          const reader = import_ait_format.AppsInTossBundle.reader(buffer);
          const appName2 = reader.appName;
          const deploymentId2 = reader.deploymentId;
          (0, import_assert.default)(typeof appName2 === "string", "ait \uD30C\uC77C\uC5D0 appName\uC774 \uC5C6\uC2B5\uB2C8\uB2E4");
          (0, import_assert.default)(typeof deploymentId2 === "string", "ait \uD30C\uC77C\uC5D0 deploymentId\uAC00 \uC5C6\uC2B5\uB2C8\uB2E4");
          const runtimeVersion = reader.metadata?.runtimeVersion;
          (0, import_assert.default)(
            typeof runtimeVersion === "string" && runtimeVersion.length > 0,
            "ait \uD30C\uC77C\uC5D0 runtimeVersion\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uBC88\uB4E4\uC744 \uB2E4\uC2DC \uBE4C\uB4DC\uD574\uC8FC\uC138\uC694."
          );
          return { appName: appName2, deploymentId: deploymentId2 };
        } else if (format === import_ait_format.AppsInTossBundle.Format.ZIP) {
          const rawAppManifest = await (0, import_utils5.readZipContent)(resolvedArtifactPath, APP_MANIFEST_NAME);
          const appManifest = JSON.parse(rawAppManifest);
          const appName2 = appManifest.appName;
          const deploymentId2 = appManifest._metadata.deploymentId;
          (0, import_assert.default)(typeof appName2 === "string", "app.json \uD30C\uC77C\uC5D0 appName\uC774 \uC5C6\uC2B5\uB2C8\uB2E4");
          (0, import_assert.default)(typeof deploymentId2 === "string", "app.json \uD30C\uC77C\uC5D0 deploymentId\uAC00 \uC5C6\uC2B5\uB2C8\uB2E4");
          const runtimeVersion = appManifest._metadata?.runtimeVersion;
          (0, import_assert.default)(
            typeof runtimeVersion === "string" && runtimeVersion.length > 0,
            "app.json \uD30C\uC77C\uC5D0 runtimeVersion\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uBC88\uB4E4\uC744 \uB2E4\uC2DC \uBE4C\uB4DC\uD574\uC8FC\uC138\uC694."
          );
          return { appName: appName2, deploymentId: deploymentId2 };
        } else {
          throw new Error("\uC9C0\uC6D0\uD558\uC9C0 \uC54A\uB294 \uD30C\uC77C \uD615\uC2DD\uC785\uB2C8\uB2E4");
        }
      })();
      const colorAppName = this.decorate(appName, import_picocolors.default.cyan);
      if (this.schemeOnly) {
        await uploadArtifact({
          artifactPath: resolvedArtifactPath,
          appName,
          deploymentId,
          apiKey,
          memo: this.memo,
          baseUrl
        });
      } else {
        await p4.tasks([
          {
            title: `${colorAppName} \uC571 \uBC30\uD3EC \uC911...`,
            task: async () => {
              await uploadArtifact({
                artifactPath: resolvedArtifactPath,
                appName,
                deploymentId,
                apiKey,
                memo: this.memo,
                baseUrl
              });
              return `${colorAppName} \uBC30\uD3EC\uAC00 \uC644\uB8CC\uB418\uC5C8\uC5B4\uC694`;
            }
          }
        ]);
      }
      this.printResult(appName, deploymentId);
    } catch (error) {
      if (error instanceof Error) {
        this.printError(error);
      } else {
        console.error(error);
      }
      process.exit(1);
    }
  }
  decorate(message, color) {
    if (this.schemeOnly) {
      return message;
    }
    return import_picocolors.default.underline(color(message));
  }
  printResult(appName, deploymentId) {
    const result = `intoss-private://${appName}?_deploymentId=${deploymentId}`;
    if (this.schemeOnly) {
      this.context.stdout.write(`${result}
`);
    } else {
      p4.note(this.decorate(result, import_picocolors.default.green));
    }
  }
  printError(error) {
    if (this.schemeOnly) {
      this.context.stdout.write(`${error.message}
`);
    } else {
      p4.log.error(error.message);
    }
  }
  async getApiKey(profile) {
    const token = TokenStorage.get(profile) || this.apiKey;
    if (token) {
      return token;
    }
    return await p4.password({
      message: "\uC571\uC778\uD1A0\uC2A4 \uBC30\uD3EC API \uD0A4\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694",
      validate: (value) => {
        if (value.length === 0) {
          return "API \uD0A4\uB294 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.";
        }
        return;
      }
    });
  }
  getArtifactPath(location) {
    if (location) {
      if (!location.endsWith(".ait")) {
        throw new Error("\uBC30\uD3EC\uD560 \uD30C\uC77C\uC740 .ait \uD655\uC7A5\uC790\uC5EC\uC57C \uD569\uB2C8\uB2E4.");
      }
      return import_path9.default.resolve(location);
    }
    const packageRoot = (0, import_utils5.getPackageRoot)();
    const artifactFile = import_fs3.default.readdirSync(packageRoot).find((file) => file.endsWith(".ait"));
    if (!artifactFile) {
      throw new Error("\uBC30\uD3EC\uD560 .ait \uD30C\uC77C\uC744 \uCC3E\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4.");
    }
    return import_path9.default.resolve(packageRoot, artifactFile);
  }
};

// src/InitCommand.ts
init_cjs_shims();
var import_fs4 = __toESM(require("fs"), 1);
var import_path10 = __toESM(require("path"), 1);
var p5 = __toESM(require("@clack/prompts"), 1);
var import_utils6 = require("@granite-js/utils");
var import_clipanion3 = require("clipanion");

// ../../../.yarn/cache/es-toolkit-npm-1.32.0-019fc7c155-56ba965570.zip/node_modules/es-toolkit/dist/index.mjs
init_cjs_shims();

// ../../../.yarn/cache/es-toolkit-npm-1.32.0-019fc7c155-56ba965570.zip/node_modules/es-toolkit/dist/string/words.mjs
init_cjs_shims();
var CASE_SPLIT_PATTERN = /\p{Lu}?\p{Ll}+|[0-9]+|\p{Lu}+(?!\p{Ll})|\p{Emoji_Presentation}|\p{Extended_Pictographic}|\p{L}+/gu;
function words(str) {
  return Array.from(str.match(CASE_SPLIT_PATTERN) ?? []);
}

// ../../../.yarn/cache/es-toolkit-npm-1.32.0-019fc7c155-56ba965570.zip/node_modules/es-toolkit/dist/string/kebabCase.mjs
init_cjs_shims();
function kebabCase(str) {
  const words$1 = words(str);
  return words$1.map((word) => word.toLowerCase()).join("-");
}

// src/templates.ts
init_cjs_shims();
var WEB_FRAMEWORK_CONFIG_TEMPLATE = `import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: '%%appName%%',
  brand: {
    displayName: '%%appName%%', // \uD654\uBA74\uC5D0 \uB178\uCD9C\uB420 \uC571\uC758 \uD55C\uAE00 \uC774\uB984\uC73C\uB85C \uBC14\uAFD4\uC8FC\uC138\uC694.
    primaryColor: '#3182F6', // \uD654\uBA74\uC5D0 \uB178\uCD9C\uB420 \uC571\uC758 \uAE30\uBCF8 \uC0C9\uC0C1\uC73C\uB85C \uBC14\uAFD4\uC8FC\uC138\uC694.
    icon: null, // \uD654\uBA74\uC5D0 \uB178\uCD9C\uB420 \uC571\uC758 \uC544\uC774\uCF58 \uC774\uBBF8\uC9C0 \uC8FC\uC18C\uB85C \uBC14\uAFD4\uC8FC\uC138\uC694.
  },
  web: {
    host: 'localhost',
    port: %%webPort%%,
    commands: {
      dev: '%%webDevCommand%%',
      build: '%%webBuildCommand%%',
    },
  },
  permissions: [],
  outdir: 'dist',
});
`;
var REACT_NATIVE_FRAMEWORK_CONFIG_TEMPLATE = `import { appsInToss } from '@apps-in-toss/framework/plugins';
import { defineConfig } from '@granite-js/react-native/config';

export default defineConfig({
  scheme: 'intoss',
  appName: '%%appName%%',
  plugins: [
    appsInToss({
      brand: {
        displayName: '%%appName%%', // \uD654\uBA74\uC5D0 \uB178\uCD9C\uB420 \uC571\uC758 \uD55C\uAE00 \uC774\uB984\uC73C\uB85C \uBC14\uAFD4\uC8FC\uC138\uC694.
        primaryColor: '#3182F6', // \uD654\uBA74\uC5D0 \uB178\uCD9C\uB420 \uC571\uC758 \uAE30\uBCF8 \uC0C9\uC0C1\uC73C\uB85C \uBC14\uAFD4\uC8FC\uC138\uC694.
        icon: null, // \uD654\uBA74\uC5D0 \uB178\uCD9C\uB420 \uC571\uC758 \uC544\uC774\uCF58 \uC774\uBBF8\uC9C0 \uC8FC\uC18C\uB85C \uBC14\uAFD4\uC8FC\uC138\uC694.
      },
      permissions: [],
    }),
  ],
});
`;
var APP_TEMPLATE = `import { AppsInToss } from '@apps-in-toss/framework';
import { PropsWithChildren } from 'react';
import { InitialProps } from '@granite-js/react-native';
import { context } from '../require.context';

function AppContainer({ children }: PropsWithChildren<InitialProps>) {
  return <>{children}</>;
}

export default AppsInToss.registerApp(AppContainer, { context });
`;

// src/utils/ensureSelect.ts
init_cjs_shims();
async function ensureSelect({
  value,
  prompt
}) {
  if (value) {
    return value;
  }
  return await prompt();
}

// src/utils/transformTemplate.ts
init_cjs_shims();
function transformTemplate(templateString, values) {
  let result = templateString;
  for (const key in values) {
    const placeholder = `%%${key}%%`;
    result = result.replace(new RegExp(placeholder, "g"), values[key]);
  }
  return result;
}

// src/InitCommand.ts
async function templateWebFramework({ appName, cwd, skipInput }) {
  const packageJson = await import_fs4.default.promises.readFile(import_path10.default.join(cwd, "package.json"), {
    encoding: "utf-8"
  });
  const scripts = JSON.parse(packageJson);
  const originalScripts = {
    dev: scripts?.scripts?.dev || scripts?.scripts?.start,
    build: scripts?.scripts?.build
  };
  scripts.scripts.dev = "granite dev";
  scripts.scripts.build = "ait build";
  scripts.scripts.deploy = "ait deploy";
  await import_fs4.default.promises.writeFile(import_path10.default.join(cwd, "package.json"), JSON.stringify(scripts, null, 2), {
    encoding: "utf-8"
  });
  p5.log.step(".gitignore \uD30C\uC77C\uC744 \uC5C5\uB370\uC774\uD2B8\uD558\uB294 \uC911...");
  await import_fs4.default.promises.appendFile(import_path10.default.join(cwd, ".gitignore"), "\n.granite\n");
  const placeholder = {
    dev: originalScripts.dev === "granite dev" ? "" : originalScripts.dev,
    build: originalScripts.build === "ait build" ? "" : originalScripts.build
  };
  const scriptInputs = skipInput ? placeholder : await p5.group(
    {
      dev: () => p5.text({
        message: "\uC6F9 \uBC88\uB4E4\uB7EC\uC758 dev \uBA85\uB839\uC5B4\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694.",
        defaultValue: placeholder.dev,
        placeholder: placeholder.dev
      }),
      build: () => p5.text({
        message: "\uC6F9 \uBC88\uB4E4\uB7EC\uC758 build \uBA85\uB839\uC5B4\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694.",
        defaultValue: placeholder.build,
        placeholder: placeholder.build
      })
    },
    {
      onCancel: () => {
        p5.cancel("\uCD08\uAE30\uD654\uAC00 \uCDE8\uC18C\uB418\uC5C8\uC2B5\uB2C8\uB2E4");
      }
    }
  );
  p5.log.step("granite.config.ts \uD30C\uC77C\uC744 \uC0DD\uC131\uD558\uB294 \uC911...");
  const webPort = skipInput ? "5173" : await p5.text({
    message: "Enter web port",
    placeholder: "5173",
    defaultValue: "5173"
  });
  if (p5.isCancel(webPort)) {
    p5.cancel("\uCD08\uAE30\uD654\uAC00 \uCDE8\uC18C\uB418\uC5C8\uC2B5\uB2C8\uB2E4");
    return;
  }
  const config = transformTemplate(WEB_FRAMEWORK_CONFIG_TEMPLATE, {
    appName,
    webPort,
    webDevCommand: scriptInputs.dev === "granite dev" ? "" : scriptInputs.dev,
    webBuildCommand: scriptInputs.build === "ait build" ? "" : scriptInputs.build
  });
  await import_fs4.default.promises.writeFile(import_path10.default.join(cwd, "granite.config.ts"), config, {
    encoding: "utf-8"
  });
}
async function templateReactNative({ appName, cwd }) {
  const packageJsonPath = import_path10.default.join(cwd, "package.json");
  const packageJsonRaw = await import_fs4.default.promises.readFile(packageJsonPath, {
    encoding: "utf-8"
  });
  const packageJson = JSON.parse(packageJsonRaw);
  packageJson.scripts ||= {};
  packageJson.scripts.dev = "granite dev";
  packageJson.scripts.build = "ait build";
  packageJson.scripts.deploy = "ait deploy";
  await import_fs4.default.promises.writeFile(packageJsonPath, JSON.stringify(packageJson, null, 2), {
    encoding: "utf-8"
  });
  p5.log.step("granite.config.ts \uD30C\uC77C\uC744 \uC0DD\uC131\uD558\uB294 \uC911...");
  const config = transformTemplate(REACT_NATIVE_FRAMEWORK_CONFIG_TEMPLATE, {
    appName
  });
  await import_fs4.default.promises.writeFile(import_path10.default.join(cwd, "granite.config.ts"), config, {
    encoding: "utf-8"
  });
  p5.log.step("src/_app.tsx \uD30C\uC77C\uC744 \uC0DD\uC131\uD558\uB294 \uC911...");
  await import_fs4.default.promises.writeFile(import_path10.default.join(cwd, "src", "_app.tsx"), APP_TEMPLATE, {
    encoding: "utf-8"
  });
}
var InitCommand = class extends import_clipanion3.Command {
  static paths = [[`init`]];
  static usage = import_clipanion3.Command.Usage({
    category: "Setup",
    description: "\uC571\uC778\uD1A0\uC2A4 \uD504\uB85C\uC81D\uD2B8 \uAC1C\uBC1C/\uBC30\uD3EC \uC124\uC815\uC744 \uCD08\uAE30\uD654\uD569\uB2C8\uB2E4.",
    examples: [
      ["\uB300\uD654\uD615\uC73C\uB85C \uCD08\uAE30\uD654", "ait init"],
      ["\uC6F9 \uD504\uB808\uC784\uC6CC\uD06C \uD15C\uD50C\uB9BF\uC73C\uB85C \uCD08\uAE30\uD654", "ait init --template web-framework --app-name my-miniapp"],
      ["RN(Granite) \uD15C\uD50C\uB9BF\uC73C\uB85C \uCD08\uAE30\uD654", "ait init --template react-native --app-name my-miniapp"]
    ]
  });
  template = import_clipanion3.Option.String("--template", {
    required: false,
    description: "\uCD08\uAE30\uD654 \uD15C\uD50C\uB9BF\uC744 \uC9C0\uC815\uD574\uC694. (web-framework | react-native)"
  });
  appName = import_clipanion3.Option.String("--app-name", {
    required: false,
    description: "\uC571 \uC774\uB984(\uCF00\uBC25-\uCF00\uC774\uC2A4)\uC744 \uC9C0\uC815\uD574\uC694. \uC608) my-miniapp"
  });
  skipInput = import_clipanion3.Option.Boolean("--skip-input", { required: false, hidden: true });
  async execute() {
    const template = await ensureSelect({
      value: this.template,
      prompt: async () => p5.select({
        message: "\uD504\uB85C\uC81D\uD2B8\uB97C \uCD08\uAE30\uD654\uD560 \uD15C\uD50C\uB9BF\uC744 \uC120\uD0DD\uD574\uC8FC\uC138\uC694.",
        options: [
          {
            value: "web-framework",
            label: "\uC6F9 \uD504\uB808\uC784\uC6CC\uD06C",
            hint: "\uC6F9 \uD504\uB85C\uC81D\uD2B8\uC5D0 \uC571\uC778\uD1A0\uC2A4 \uC6F9\uBDF0 \uAC1C\uBC1C\uD658\uACBD\uC744 \uC138\uD305\uD569\uB2C8\uB2E4."
          },
          {
            value: "react-native",
            label: "React Native \uD504\uB808\uC784\uC6CC\uD06C (Granite)",
            hint: "Granite \uD504\uB85C\uC81D\uD2B8\uC5D0 \uC571\uC778\uD1A0\uC2A4 React Native\uC6A9 \uAC1C\uBC1C\uD658\uACBD\uC744 \uC138\uD305\uD569\uB2C8\uB2E4."
          }
        ]
      })
    });
    if (p5.isCancel(template)) {
      process.exit(0);
    }
    const cwd = (0, import_utils6.getPackageRoot)();
    p5.intro("\u{1F680} \uC571 \uCD08\uAE30\uD654\uB97C \uC2DC\uC791\uD569\uB2C8\uB2E4");
    const appName = await ensureSelect({
      value: this.appName,
      prompt: async () => p5.text({
        message: "Enter app name",
        validate: (value) => {
          if (!value) {
            return "\uC571 \uC774\uB984\uC744 \uC785\uB825\uD574\uC8FC\uC138\uC694";
          }
          const kebabCaseValue = kebabCase(value);
          if (value !== kebabCaseValue) {
            return `\uC571 \uC774\uB984\uC740 \uCF00\uBC25-\uCF00\uC774\uC2A4 \uD615\uC2DD\uC774\uC5B4\uC57C \uD569\uB2C8\uB2E4 (\uC608\uC2DC: ${kebabCaseValue})`;
          }
          return;
        }
      })
    });
    if (p5.isCancel(appName)) {
      p5.cancel("\uCD08\uAE30\uD654\uAC00 \uCDE8\uC18C\uB418\uC5C8\uC2B5\uB2C8\uB2E4");
      return;
    }
    p5.log.step(`\uC571 \uC774\uB984\uC774 '${appName}'\uC73C\uB85C \uC124\uC815\uB418\uC5C8\uC2B5\uB2C8\uB2E4`);
    switch (template) {
      case "web-framework": {
        await templateWebFramework({
          appName,
          cwd,
          skipInput: this.skipInput ?? false
        });
        break;
      }
      case "react-native": {
        await templateReactNative({
          appName,
          cwd
        });
        break;
      }
    }
    p5.outro("\u2728 \uCD08\uAE30\uD654\uAC00 \uC644\uB8CC\uB418\uC5C8\uC2B5\uB2C8\uB2E4!");
  }
};

// src/MigrateCommand/index.ts
init_cjs_shims();

// src/MigrateCommand/MigrateCommand.ts
init_cjs_shims();
var import_clipanion4 = require("clipanion");

// src/MigrateCommand/logger/index.ts
init_cjs_shims();
var import_picocolors2 = __toESM(require_picocolors(), 1);
var Logger = class {
  prefix = import_picocolors2.default.bold(import_picocolors2.default.bgCyan(import_picocolors2.default.black(" MIGRATE ")));
  log(...args) {
    console.log(this.prefix, ...args);
  }
  success(...args) {
    console.log(this.prefix, import_picocolors2.default.green("\u2713"), ...args);
  }
  error(...args) {
    console.error(this.prefix, import_picocolors2.default.red("\u2717"), ...args);
  }
  warn(...args) {
    console.warn(this.prefix, import_picocolors2.default.yellow("!"), ...args);
  }
};
var logger = new Logger();

// src/MigrateCommand/presets/index.ts
init_cjs_shims();

// src/MigrateCommand/presets/granite/index.ts
init_cjs_shims();

// src/MigrateCommand/presets/granite/migrateGranite.ts
init_cjs_shims();
var import_assert2 = __toESM(require("assert"), 1);
var import_dedent = __toESM(require("dedent"), 1);

// src/MigrateCommand/presets/granite/configProcessor.ts
init_cjs_shims();

// src/MigrateCommand/fs/fileProcessor.ts
init_cjs_shims();
var import_fs5 = require("fs");
async function readFile(filePath) {
  try {
    return await import_fs5.promises.readFile(filePath, "utf-8");
  } catch {
    throw new Error(`Failed to read file: ${filePath}`);
  }
}
async function writeFile(filePath, content) {
  try {
    await import_fs5.promises.writeFile(filePath, content, "utf-8");
  } catch {
    throw new Error(`Failed to write file: ${filePath}`);
  }
}
async function fileExists(filePath) {
  try {
    await import_fs5.promises.access(filePath);
    return true;
  } catch {
    return false;
  }
}
async function renameFile(oldPath, newPath) {
  try {
    await import_fs5.promises.rename(oldPath, newPath);
  } catch {
    throw new Error(`Failed to rename file: ${oldPath} -> ${newPath}`);
  }
}

// src/MigrateCommand/presets/granite/configProcessor.ts
var configTransformer = (root, { j }) => {
  root.find(j.ImportDeclaration).forEach((path13) => {
    const source = path13.value.source;
    if (j.StringLiteral.check(source) && source.value === "react-native-bedrock/config") {
      const defineConfigName = path13.value.specifiers?.find(
        (specifier) => j.ImportSpecifier.check(specifier) && specifier.imported.name === "defineConfig"
      )?.local?.name;
      if (!defineConfigName) {
        return;
      }
      root.find(j.CallExpression).forEach((path14) => {
        const callee = path14.value.callee;
        if (j.Identifier.check(callee) && callee.name === defineConfigName && path14.value.arguments.length > 0 && j.ObjectExpression.check(path14.value.arguments[0])) {
          const obj = path14.value.arguments[0];
          const hasScheme = obj.properties.some(
            (prop) => j.ObjectProperty.check(prop) && j.Identifier.check(prop.key) && prop.key.name === "scheme"
          );
          if (!hasScheme) {
            obj.properties.unshift(j.objectProperty(j.identifier("scheme"), j.stringLiteral("intoss")));
          }
          const routerImport = root.find(j.ImportDeclaration).filter((path15) => {
            const source2 = path15.value.source;
            return j.StringLiteral.check(source2) && source2.value === "@granite-js/plugin-router";
          });
          if (routerImport.length === 0) {
            const firstImport = root.find(j.ImportDeclaration).at(0);
            if (firstImport.length > 0) {
              firstImport.insertAfter(
                j.importDeclaration(
                  [j.importSpecifier(j.identifier("router"))],
                  j.stringLiteral("@granite-js/plugin-router")
                )
              );
            }
          }
          const hasPlugins = obj.properties.some(
            (prop) => j.ObjectProperty.check(prop) && j.Identifier.check(prop.key) && prop.key.name === "plugins"
          );
          const routerPluginCallExpr = j.callExpression(j.identifier("router"), []);
          if (hasPlugins) {
            const pluginsProp = obj.properties.find(
              (prop) => j.ObjectProperty.check(prop) && j.Identifier.check(prop.key) && prop.key.name === "plugins"
            );
            if (j.ObjectProperty.check(pluginsProp) && j.ArrayExpression.check(pluginsProp.value)) {
              const hasRouter = pluginsProp.value.elements.some(
                (element) => j.Identifier.check(element) && element.name === "router"
              );
              if (!hasRouter) {
                pluginsProp.value.elements.push(routerPluginCallExpr);
              }
            }
          } else {
            obj.properties.unshift(
              j.objectProperty(j.identifier("plugins"), j.arrayExpression([routerPluginCallExpr]))
            );
          }
        }
      });
    }
  });
  return root;
};
var processBedrockConfig = async (cwd, config) => {
  logger.log("Processing bedrock.config.ts...");
  const bedrockConfigPath = `${cwd}/bedrock.config.ts`;
  const graniteConfigPath = `${cwd}/granite.config.ts`;
  if (!await fileExists(bedrockConfigPath)) {
    logger.log("No bedrock.config.ts found");
    return;
  }
  if (config.dryRun) {
    return;
  }
  try {
    await renameFile(bedrockConfigPath, graniteConfigPath);
    logger.success("Renamed bedrock.config.ts to granite.config.ts");
  } catch (error) {
    logger.error(`Failed to process bedrock.config.ts: ${error}`);
  }
};

// src/MigrateCommand/presets/granite/gitignoreProcessor.ts
init_cjs_shims();
var processGitignore = async (cwd, config) => {
  let modified = false;
  logger.log("Processing .gitignore...");
  const gitignorePath = `${cwd}/.gitignore`;
  try {
    if (await fileExists(gitignorePath)) {
      const content = await readFile(gitignorePath);
      const newContent = content.replace(/\.bedrock/g, ".granite");
      if (newContent !== content) {
        if (config.dryRun) {
          logger.log("Skipping file write in dry run mode");
        } else {
          await writeFile(gitignorePath, newContent);
          logger.success("Updated .gitignore");
        }
        modified ||= true;
      } else {
        logger.log(`No changes needed in ${gitignorePath}`);
      }
    }
  } catch (error) {
    logger.error(`Failed to process .gitignore: ${error}`);
  }
  if (!modified) {
    logger.log("No .gitignore found");
    return;
  }
};

// src/MigrateCommand/presets/granite/identifierReplacements.ts
init_cjs_shims();

// src/MigrateCommand/codemod/utils.ts
init_cjs_shims();
function getIdentifierName(j, id) {
  if (typeof id === "string") {
    return id;
  }
  if (j.Identifier.check(id)) {
    return id.name;
  }
  return "";
}

// src/MigrateCommand/presets/granite/identifierReplacements.ts
var identifierReplacementsTransformer = (root, { j }) => {
  const importedVariables = /* @__PURE__ */ new Set();
  function toGraniteName(name) {
    let newName = null;
    if (name === "BedrockRoute") {
      newName = "createRoute";
    } else if (name.startsWith("bedrock")) {
      newName = toGraniteCamel(name);
    } else if (name.startsWith("Bedrock")) {
      newName = toGranitePascal(name);
    } else if (name.startsWith("BEDROCK")) {
      newName = toGraniteSnake(name);
    }
    return newName;
  }
  function toGraniteCamel(name) {
    const rest = name.substring(7);
    return "granite" + rest;
  }
  function toGranitePascal(name) {
    const rest = name.substring(7);
    return "Granite" + rest;
  }
  function toGraniteSnake(name) {
    const rest = name.substring(7);
    return "GRANITE" + rest;
  }
  root.find(j.ImportDeclaration, { source: { value: "react-native-bedrock" } }).forEach((importPath) => {
    importPath.value.specifiers?.forEach((specifier) => {
      if (j.ImportSpecifier.check(specifier)) {
        const importedName = getIdentifierName(j, specifier.imported);
        const localName = specifier.local ? getIdentifierName(j, specifier.local) : importedName;
        if (j.Identifier.check(specifier.imported)) {
          const newName = toGraniteName(importedName);
          if (newName) {
            specifier.imported.name = newName;
          }
        }
        importedVariables.add(localName);
      }
    });
  });
  root.find(j.Identifier).forEach((path13) => {
    const name = path13.value.name;
    if (importedVariables.has(name)) {
      const newName = toGraniteName(path13.value.name);
      if (newName) {
        path13.value.name = newName;
      }
    }
  });
  return root;
};

// src/MigrateCommand/presets/granite/isBedrockProject.ts
init_cjs_shims();
var fs11 = __toESM(require("fs"), 1);
var BASE_NAME = "bedrock.config";
var EXTENSIONS = ["ts", "mts", "js", "cjs"];
async function isBedrockProject(cwd) {
  const files = fs11.readdirSync(cwd);
  return files.some(isBedrockConfigFile);
}
function isBedrockConfigFile(file) {
  return EXTENSIONS.some((ext) => file === `${BASE_NAME}.${ext}`);
}

// src/MigrateCommand/presets/granite/packageImports.ts
init_cjs_shims();
var import_picocolors3 = __toESM(require_picocolors(), 1);

// src/MigrateCommand/presets/granite/constants.ts
init_cjs_shims();
var UNSUPPORTED_LIBRARIES = [
  "@react-native-bedrock/e2e-testing-library",
  "@react-native-bedrock/babel-plugin-obfuscate"
];
var TARGET_VERSION = {
  granitePackages: "0.1.21",
  appsInTossPackages: "^1.0.0",
  tossDesignSystem: "^1.0.4"
};

// src/MigrateCommand/presets/granite/packageImports.ts
var packageImportsTransformer = (root, { j }, { fileName = "<unknown>" }) => {
  function transformPackageName(value) {
    if (UNSUPPORTED_LIBRARIES.includes(value)) {
      const libName = import_picocolors3.default.gray(`(${value})`);
      logger.warn(`Unsupported library found ${libName}`);
      logger.warn(`\u2514\u2500 ${import_picocolors3.default.gray(`at ${fileName}`)}`);
      return value;
    }
    if (value === "react-native-bedrock" || value.startsWith("react-native-bedrock/")) {
      return value.replace("react-native-bedrock", "@granite-js/react-native");
    }
    if (value.startsWith("@react-native-bedrock/")) {
      return value.replace("@react-native-bedrock/", "@granite-js/");
    }
    return value;
  }
  root.find(j.ImportDeclaration).forEach((path13) => {
    const source = path13.value.source;
    if (j.StringLiteral.check(source)) {
      const transformed = transformPackageName(source.value);
      if (transformed !== source.value) {
        source.value = transformed;
      }
    }
  });
  root.find(j.CallExpression, {
    callee: { name: "require" }
  }).forEach((path13) => {
    const args = path13.value.arguments;
    if (args.length > 0 && j.StringLiteral.check(args[0])) {
      const transformed = transformPackageName(args[0].value);
      if (transformed !== args[0].value) {
        args[0].value = transformed;
      }
    }
  });
  root.find(j.ImportExpression).forEach((path13) => {
    const source = path13.value.source;
    if (j.StringLiteral.check(source)) {
      const transformed = transformPackageName(source.value);
      if (transformed !== source.value) {
        source.value = transformed;
      }
    }
  });
  return root;
};

// src/MigrateCommand/presets/granite/packageJsonProcessor.ts
init_cjs_shims();
var import_picocolors4 = __toESM(require_picocolors(), 1);
var processPackageJson = async (cwd, config) => {
  logger.log("Processing package.json...");
  const packageJsonPath = `${cwd}/package.json`;
  if (!await fileExists(packageJsonPath)) {
    logger.log("No package.json found");
    return;
  }
  try {
    const content = await readFile(packageJsonPath);
    const packageJson = JSON.parse(content);
    let modified = false;
    const depFields = ["dependencies", "devDependencies", "peerDependencies"];
    for (const field of depFields) {
      if (packageJson[field]) {
        const deps = packageJson[field];
        const newDeps = {
          "@granite-js/plugin-router": TARGET_VERSION.granitePackages
        };
        for (const [originLibName, originLibVersion] of Object.entries(deps)) {
          let libName = originLibName;
          let libVersion = originLibVersion;
          if (originLibName === "react-native-bedrock") {
            libName = "@granite-js/react-native";
            libVersion = TARGET_VERSION.granitePackages;
          } else if (originLibName.startsWith("@react-native-bedrock/")) {
            if (UNSUPPORTED_LIBRARIES.includes(originLibName)) {
              const packageName = import_picocolors4.default.gray(`(${originLibName})`);
              logger.warn(`Unsupported library found in package.json ${packageName}`);
            } else {
              libName = originLibName.replace("@react-native-bedrock", "@granite-js");
              libVersion = TARGET_VERSION.granitePackages;
            }
          }
          if (libName.startsWith("@apps-in-toss/")) {
            libVersion = TARGET_VERSION.appsInTossPackages;
          } else if (libName === "@toss-design-system/react-native") {
            libVersion = TARGET_VERSION.tossDesignSystem;
          }
          newDeps[libName] = libVersion;
          modified ||= libName !== originLibName;
        }
        packageJson[field] = newDeps;
      }
    }
    if (packageJson.scripts) {
      const scripts = packageJson.scripts;
      for (const [key, value] of Object.entries(scripts)) {
        if (typeof value !== "string") {
          continue;
        }
        let nextValue = value;
        if (nextValue.includes("bedrock")) {
          nextValue = nextValue.replace(/\bbedrock\b/g, "granite");
        }
        if (nextValue.includes("granite build")) {
          nextValue = nextValue.replace(/granite build/g, "ait build");
        }
        if (nextValue !== value) {
          scripts[key] = nextValue;
          modified = true;
        }
      }
    }
    if (config.dryRun) {
      return;
    }
    if (modified) {
      await writeFile(packageJsonPath, JSON.stringify(packageJson, null, 2) + "\n");
      logger.success("Updated package.json");
    } else {
      logger.log("No changes needed in package.json");
    }
  } catch (error) {
    logger.error(`Failed to process package.json: ${error}`);
  }
};

// src/MigrateCommand/presets/granite/toAppsInTossFrameworkImport.ts
init_cjs_shims();
var APPS_IN_TOSS_FRAMEWORK_EXPORTS = [
  "closeView",
  "getLocale",
  "getSchemeUri",
  "generateHapticFeedback",
  "share",
  "setSecureScreen",
  "setScreenAwakeMode",
  "getNetworkStatus",
  "setIosSwipeGestureEnabled",
  "openURL",
  "getPlatformOS"
];
var toAppsInTossFrameworkImport = (root, { j }, { fileName = "<unknown>" }) => {
  const appsInTossFrameworkMembers = [];
  root.find(j.ImportDeclaration).forEach((path13) => {
    const source = path13.node.source.value;
    if (typeof source !== "string") {
      return;
    }
    if (source === "@granite-js/react-native") {
      if (Array.isArray(path13.node.specifiers)) {
        path13.node.specifiers = path13.node.specifiers.filter((specifier) => {
          if (shouldImportFromAppsInTossFramework(fileName, specifier)) {
            appsInTossFrameworkMembers.push(specifier);
            return false;
          }
          return true;
        });
      }
      if (path13.node.specifiers?.length === 0) {
        j(path13).remove();
      }
    }
  });
  if (appsInTossFrameworkMembers.length > 0) {
    const importStatement = j.importDeclaration(appsInTossFrameworkMembers, j.literal("@apps-in-toss/framework"));
    root.find(j.Program).get("body", 0).insertBefore(importStatement);
  }
  return root;
};
function shouldImportFromAppsInTossFramework(fileName, specifier) {
  switch (specifier.type) {
    case "ImportSpecifier":
      return APPS_IN_TOSS_FRAMEWORK_EXPORTS.includes(String(specifier.imported.name));
    case "ImportDefaultSpecifier":
    case "ImportNamespaceSpecifier":
    default:
      logger.warn(`Unknown import specifier in ${fileName}`);
      return false;
  }
}

// src/MigrateCommand/fs/fileScanner.ts
init_cjs_shims();
var import_fast_glob = __toESM(require("fast-glob"), 1);
var DEFAULT_EXTENSIONS = ["js", "ts", "jsx", "tsx", "cjs", "mjs"];
var DEFAULT_EXCLUDE_PATTERNS = ["**/node_modules/**", "**/.git/**", "**/dist/**"];
async function scanFiles(directory, options = {}) {
  const { extensions = DEFAULT_EXTENSIONS, excludePatterns = DEFAULT_EXCLUDE_PATTERNS } = options;
  const patterns = extensions.map((ext) => `**/*.${ext}`);
  const files = await (0, import_fast_glob.default)(patterns, {
    cwd: directory,
    absolute: true,
    ignore: excludePatterns
  });
  return files;
}

// src/MigrateCommand/presets/granite/migrateGranite.ts
var transformers = [
  configTransformer,
  identifierReplacementsTransformer,
  packageImportsTransformer,
  toAppsInTossFrameworkImport
];
var migrateGranite = async (cwd, config) => {
  (0, import_assert2.default)(isBedrockProject(cwd), "This is not a Bedrock project");
  let processedCount = 0;
  const files = await scanFiles(cwd);
  logger.log(`Found ${files.length} source files`);
  logger.log("Processing source files...");
  for (const file of files) {
    try {
      const content = await readFile(file);
      const newContent = await transform(content, {
        transformers,
        options: {
          fileName: file
        }
      });
      if (config.dryRun !== true && newContent !== content) {
        await writeFile(file, newContent);
        processedCount++;
      }
    } catch (error) {
      logger.error(`Failed to process file: ${file}`, error);
    }
  }
  if (processedCount > 0) {
    logger.success(`Processed ${processedCount} source files`);
  } else {
    logger.log("No source files needed processing");
  }
  await processPackageJson(cwd, config);
  await processGitignore(cwd, config);
  await processBedrockConfig(cwd, config);
  logger.log(import_dedent.default`
  Please check the following steps:

  - Install dependencies
  - Run type checks
  - Run lint checks if a lint config exists
  - Verify test results if tests are present\n
  `);
};

// src/MigrateCommand/presets/react-native-0-84-0/index.ts
init_cjs_shims();

// src/MigrateCommand/presets/react-native-0-84-0/migrateReactNative0840.ts
init_cjs_shims();
var import_dedent2 = __toESM(require("dedent"), 1);
var TARGET_DEPENDENCY_VERSIONS = {
  react: "19.2.3",
  "react-dom": "19.2.3",
  "react-native": "0.84.0",
  "@types/react": "19.2.3",
  "@types/react-dom": "19.2.3",
  "react-test-renderer": "19.2.3",
  "babel-preset-granite": "1.0.18",
  "@babel/plugin-transform-flow-strip-types": "7.27.1",
  "@granite-js/react-native": "1.0.18",
  "@granite-js/native": "1.0.18",
  "@granite-js/plugin-core": "1.0.18",
  "@granite-js/plugin-env": "1.0.18",
  "@granite-js/plugin-hermes": "1.0.18",
  "@granite-js/plugin-router": "1.0.18",
  "@granite-js/plugin-micro-frontend": "1.0.18",
  "@granite-js/plugin-sentry": "1.0.18",
  "@toss/tds-react-native": "2.0.3",
  "brick-module": "0.5.0"
};
var BABEL_CONFIG_TEMPLATE = `module.exports = {
  presets: ['babel-preset-granite'],
};
`;
var REQUIRED_DEPENDENCIES_BY_FIELD = {
  dependencies: {
    "brick-module": TARGET_DEPENDENCY_VERSIONS["brick-module"]
  },
  devDependencies: {
    "babel-preset-granite": TARGET_DEPENDENCY_VERSIONS["babel-preset-granite"]
  }
};
var DEPENDENCY_FIELDS = ["dependencies", "devDependencies", "peerDependencies"];
function processDependencyMap(deps, frameworkVersion) {
  let modified = false;
  for (const [name, version] of Object.entries(deps)) {
    if (name.startsWith("@apps-in-toss/")) {
      if (name === "@apps-in-toss/framework" || frameworkVersion == null) {
        continue;
      }
      if (version !== frameworkVersion) {
        deps[name] = frameworkVersion;
        modified = true;
      }
      continue;
    }
    const targetVersion = TARGET_DEPENDENCY_VERSIONS[name];
    if (targetVersion == null || version === targetVersion) {
      continue;
    }
    if (version !== targetVersion) {
      deps[name] = targetVersion;
      modified = true;
    }
  }
  return modified;
}
function findDependencyVersion(packageJson, dependencyName) {
  for (const field of DEPENDENCY_FIELDS) {
    const deps = packageJson[field];
    if (deps?.[dependencyName] != null) {
      return deps[dependencyName];
    }
  }
  return void 0;
}
function hasReactNativeDependency(packageJson) {
  return findDependencyVersion(packageJson, "react-native") != null;
}
function processScriptMap(scripts) {
  let modified = false;
  for (const [scriptName, scriptValue] of Object.entries(scripts)) {
    if (!scriptValue.includes("granite build")) {
      continue;
    }
    const nextScriptValue = scriptValue.replace(/granite build/g, "ait build");
    if (nextScriptValue !== scriptValue) {
      scripts[scriptName] = nextScriptValue;
      modified = true;
    }
  }
  return modified;
}
function ensureRequiredDependencies(packageJson) {
  let modified = false;
  for (const field of DEPENDENCY_FIELDS) {
    const requiredDependencies = REQUIRED_DEPENDENCIES_BY_FIELD[field];
    if (requiredDependencies == null) {
      continue;
    }
    packageJson[field] ??= {};
    const deps = packageJson[field];
    if (deps == null) {
      continue;
    }
    for (const [dependencyName, requiredVersion] of Object.entries(requiredDependencies)) {
      if (deps[dependencyName] === requiredVersion) {
        continue;
      }
      deps[dependencyName] = requiredVersion;
      modified = true;
    }
  }
  return modified;
}
async function processPackageJson2(cwd, config) {
  logger.log("Processing package.json...");
  const packageJsonPath = `${cwd}/package.json`;
  if (!await fileExists(packageJsonPath)) {
    logger.log("No package.json found");
    return false;
  }
  try {
    const content = await readFile(packageJsonPath);
    const packageJson = JSON.parse(content);
    if (!hasReactNativeDependency(packageJson)) {
      logger.log("Skipping migration: react-native dependency not found in package.json");
      return false;
    }
    const appsInTossFrameworkVersion = findDependencyVersion(packageJson, "@apps-in-toss/framework");
    let modified = false;
    for (const field of DEPENDENCY_FIELDS) {
      if (packageJson[field] == null) {
        continue;
      }
      modified = processDependencyMap(packageJson[field], appsInTossFrameworkVersion) || modified;
    }
    modified = ensureRequiredDependencies(packageJson) || modified;
    if (packageJson.scripts != null) {
      modified = processScriptMap(packageJson.scripts) || modified;
    }
    if (!modified) {
      logger.log("No changes needed in package.json");
      return true;
    }
    if (config.dryRun === true) {
      logger.log("Skipping package.json write in dry run mode");
      return true;
    }
    await writeFile(packageJsonPath, JSON.stringify(packageJson, null, 2) + "\n");
    logger.success("Updated package.json");
    return true;
  } catch (error) {
    logger.error(`Failed to process package.json: ${error}`);
    return false;
  }
}
function isTargetBabelConfig(content) {
  return content.trim() === BABEL_CONFIG_TEMPLATE.trim();
}
async function processBabelConfig(cwd, config) {
  logger.log("Processing babel.config.js...");
  const babelConfigPath = `${cwd}/babel.config.js`;
  if (!await fileExists(babelConfigPath)) {
    logger.log("No babel.config.js found");
    return;
  }
  try {
    const content = await readFile(babelConfigPath);
    if (isTargetBabelConfig(content)) {
      logger.log("No changes needed in babel.config.js");
      return;
    }
    if (config.dryRun === true) {
      logger.log("Skipping babel.config.js write in dry run mode");
      return;
    }
    await writeFile(babelConfigPath, BABEL_CONFIG_TEMPLATE);
    logger.success("Updated babel.config.js");
  } catch (error) {
    logger.error(`Failed to process babel.config.js: ${error}`);
  }
}
var migrateReactNative0840 = async (cwd, config) => {
  const isReactNativeProject = await processPackageJson2(cwd, config);
  if (!isReactNativeProject) {
    logger.log("Skipping remaining migration steps");
    return;
  }
  await processBabelConfig(cwd, config);
  logger.log(import_dedent2.default`
  Please check the following steps:

  - Install dependencies
  - Run type checks
  - Run lint checks
  - Run tests
  - Review migration warnings manually (if any)\n
  `);
};

// src/MigrateCommand/presets/index.ts
var MIGRATE_PRESETS = {
  granite: migrateGranite,
  "react-native-0-84-0": migrateReactNative0840
};

// src/MigrateCommand/MigrateCommand.ts
function getAvailablePresetTargets() {
  return Object.keys(MIGRATE_PRESETS).sort();
}
function logAvailablePresets(level = "log") {
  const output = level === "error" ? logger.error.bind(logger) : logger.log.bind(logger);
  output("Available migration presets:");
  for (const target of getAvailablePresetTargets()) {
    output(`- ${target}`);
  }
}
var MigrateCommand = class extends import_clipanion4.Command {
  static paths = [[`migrate`]];
  static usage = import_clipanion4.Command.Usage({
    category: "Migration",
    description: "Run migration",
    examples: [
      ["Run migration", "ait migrate <target>"],
      ["Check migration availability", "ait migrate"],
      ["Dry run migration", "ait migrate <target> --dry-run"]
    ]
  });
  target = import_clipanion4.Option.String({ required: false });
  dryRun = import_clipanion4.Option.Boolean("--dry-run", { required: false });
  async execute() {
    try {
      if (this.target == null) {
        logAvailablePresets();
        return 0;
      }
      const cwd = process.cwd();
      const config = {
        dryRun: this.dryRun
      };
      logger.log("Starting migration...");
      logger.log(`Working directory: ${cwd}`);
      const migrate = MIGRATE_PRESETS[this.target];
      if (migrate == null) {
        logger.error(`Unknown target: ${this.target}`);
        logAvailablePresets("error");
        return 1;
      }
      await migrate(cwd, config);
      logger.success("Migration completed successfully!");
      return 0;
    } catch (error) {
      logger.error(`Migration failed: ${error.message}`);
      return 1;
    }
  }
};

// src/TokenCommand.ts
init_cjs_shims();
var p6 = __toESM(require("@clack/prompts"), 1);
var import_clipanion5 = require("clipanion");
var import_debug3 = __toESM(require_src(), 1);
var debug3 = (0, import_debug3.default)("cli:token");
var TokenCommand = class extends import_clipanion5.Command {
  static paths = [["token"]];
  static usage = import_clipanion5.Command.Usage({
    category: "Auth",
    description: "\uD1A0\uD070 \uAD00\uB828 \uD558\uC704 \uBA85\uB839\uC744 \uAD00\uB9AC\uD569\uB2C8\uB2E4.",
    examples: [
      ["\uD1A0\uD070 \uCD94\uAC00", "ait token add <profile?>"],
      ["\uD1A0\uD070 \uC0AD\uC81C", "ait token remove <profile?>"]
    ]
  });
  async execute() {
    debug3("execute");
  }
};
var TokenAddCommand = class extends import_clipanion5.Command {
  static paths = [["token", "add"]];
  static usage = import_clipanion5.Command.Usage({
    category: "Auth",
    description: "\uC2DC\uD06C\uB9BF \uD1A0\uD070\uC744 \uCD94\uAC00\uD569\uB2C8\uB2E4.",
    examples: [
      ["\uAE30\uBCF8 \uBCC4\uCE6D\uC73C\uB85C \uD1A0\uD070 \uCD94\uAC00", "ait token add"],
      ["\uBCC4\uCE6D\uC744 \uC9C0\uC815\uD558\uC5EC \uD1A0\uD070 \uCD94\uAC00", "ait token add dev"]
    ]
  });
  profile = import_clipanion5.Option.String({ required: false });
  apiKey = import_clipanion5.Option.String("--api-key", { required: false });
  async execute() {
    const profile = this.profile || "default";
    const secret = this.apiKey ? this.apiKey : await p6.password({
      message: "Enter secret token:",
      validate: (value) => {
        if (value.length === 0) {
          return "\uD1A0\uD070\uC740 \uBE44\uC5B4 \uC788\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4.";
        }
        return;
      }
    });
    if (p6.isCancel(secret)) {
      return;
    }
    TokenStorage.set(profile, secret);
    debug3("token add", { profile });
    this.context.stdout.write(`${profile} \uD504\uB85C\uD544\uB85C\uC758 \uC694\uCCAD\uC740 \uC774\uC81C \uBE44\uBC00 \uD1A0\uD070\uC744 \uC0AC\uC6A9\uD558\uC5EC \uC778\uC99D\uB429\uB2C8\uB2E4.
`);
  }
};
var TokenRemoveCommand = class extends import_clipanion5.Command {
  static paths = [["token", "remove"]];
  static usage = import_clipanion5.Command.Usage({
    category: "Auth",
    description: "\uC2DC\uD06C\uB9BF \uD1A0\uD070\uC744 \uC0AD\uC81C\uD569\uB2C8\uB2E4.",
    examples: [
      ["\uAE30\uBCF8 \uC6CC\uD06C\uC2A4\uD398\uC774\uC2A4\uC758 \uD1A0\uD070 \uC0AD\uC81C", "ait token remove"],
      ["\uBCC4\uCE6D\uC744 \uC9C0\uC815\uD558\uC5EC \uD1A0\uD070 \uC0AD\uC81C", "ait token remove dev"]
    ]
  });
  profile = import_clipanion5.Option.String({ required: false });
  async execute() {
    const profile = this.profile || "default";
    const removed = TokenStorage.delete(profile);
    debug3("token remove", { profile });
    if (removed) {
      this.context.stdout.write(`\uD1A0\uD070\uC744 \uC81C\uAC70\uD588\uC2B5\uB2C8\uB2E4: ${profile}.
`);
    } else {
      this.context.stdout.write(`\uD1A0\uD070\uC744 \uCC3E\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4: ${profile}.
`);
    }
  }
};

// src/integrations/UploadSentrySourcemap/index.ts
init_cjs_shims();

// src/integrations/UploadSentrySourcemap/UploadSentrySourcemap.ts
init_cjs_shims();
var fs15 = __toESM(require("fs/promises"), 1);
var p7 = __toESM(require("@clack/prompts"), 1);
var import_clipanion6 = require("clipanion");

// src/integrations/UploadSentrySourcemap/downloadArtifacts.ts
init_cjs_shims();
var fs12 = __toESM(require("fs"), 1);
var os2 = __toESM(require("os"), 1);
var path11 = __toESM(require("path"), 1);
var import_stream = require("stream");
var import_util = require("util");
var import_debug4 = __toESM(require_src(), 1);
var debug4 = (0, import_debug4.default)("integrations:sentry");
var streamPipeline = (0, import_util.promisify)(import_stream.pipeline);
async function downloadArtifacts(options) {
  const { appName, deploymentId, apiKey, baseUrl = DEFAULT_API_BASE_URL } = options;
  const params = new URLSearchParams({
    appName,
    deploymentId
  });
  const requestUrl = `${baseUrl}/api-public/v3/openapi/bundles/download?${params.toString()}`;
  debug4("Request URL", { requestUrl });
  const response = await fetch(`${baseUrl}/api-public/v3/openapi/bundles/download?${params.toString()}`, {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${apiKey}`
    }
  });
  if (!response.ok) {
    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
  }
  if (response.body == null) {
    throw new Error("No response body");
  }
  const tmpDir = await createTempDirectory();
  const tmpFile = `${deploymentId}.zip`;
  const tmpFilePath = path11.join(tmpDir.path, tmpFile);
  const fileStream = fs12.createWriteStream(tmpFilePath);
  await streamPipeline(response.body, fileStream);
  debug4("Artifacts downloaded", { tmpFilePath });
  return tmpFilePath;
}
function createTempDirectory() {
  const tempDir = path11.join(os2.tmpdir(), "apps-in-toss", "sentry-integration");
  fs12.mkdirSync(tempDir, { recursive: true });
  debug4("Temp directory created", { tempDir });
  return {
    path: tempDir,
    cleanup: () => {
      fs12.rmSync(tempDir, { recursive: true, force: true });
    }
  };
}

// src/integrations/UploadSentrySourcemap/extractSentryDebugId.ts
init_cjs_shims();
var import_promises7 = __toESM(require("fs/promises"), 1);
async function extractSentryDebugId(bundlePath) {
  const bundleContent = await import_promises7.default.readFile(bundlePath, "utf-8");
  return bundleContent.match(/SENTRY_DEBUG_ID=([\w-]+)/)?.[1];
}

// src/integrations/UploadSentrySourcemap/extractSources.ts
init_cjs_shims();
var fs14 = __toESM(require("fs"), 1);
var path12 = __toESM(require("path"), 1);
var import_stream2 = require("stream");
var import_util2 = require("util");
var zlib = __toESM(require("zlib"), 1);
var unzipper = __toESM(require("unzipper"), 1);
var pipe = (0, import_util2.promisify)(import_stream2.pipeline);
async function extractSources(zipPath) {
  const sources = {
    android: {
      packagerBundle: "",
      packagerSourceMap: "",
      hermesBundle: "",
      hermesSourceMap: ""
    },
    ios: {
      packagerBundle: "",
      packagerSourceMap: "",
      hermesBundle: "",
      hermesSourceMap: ""
    }
  };
  await fs14.createReadStream(zipPath).pipe(unzipper.Parse()).on("entry", async (entry) => {
    const isRoot = !entry.path.includes("/");
    const fileName = path12.basename(entry.path);
    const outputPath = path12.join(path12.dirname(zipPath), fileName);
    if (isRoot) {
      entry.pipe(fs14.createWriteStream(outputPath));
      const platform = fileName.includes("android") ? "android" : "ios";
      if (fileName.endsWith(".js")) {
        sources[platform].packagerBundle = outputPath;
      } else if (fileName.endsWith(".hbc.gz")) {
        sources[platform].hermesBundle = outputPath;
      } else if (fileName.endsWith(".js.map")) {
        sources[platform].packagerSourceMap = outputPath;
      } else if (fileName.endsWith(".hbc.map")) {
        sources[platform].hermesSourceMap = outputPath;
      }
    } else {
      entry.autodrain();
    }
  }).promise();
  if (Object.values({ ...sources.android, ...sources.ios }).some((source) => source === "")) {
    throw new Error("invalid artifact");
  }
  const [androidHermesBundle, iosHermesBundle] = await Promise.all([
    decompressGzip(sources.android.hermesBundle),
    decompressGzip(sources.ios.hermesBundle)
  ]);
  sources.android.hermesBundle = androidHermesBundle;
  sources.ios.hermesBundle = iosHermesBundle;
  return sources;
}
async function decompressGzip(gzFile) {
  const outfile = gzFile.replace(/\.gz$/g, "");
  await pipe(fs14.createReadStream(gzFile), zlib.createGunzip(), fs14.createWriteStream(outfile));
  return outfile;
}

// src/integrations/UploadSentrySourcemap/injectSentryDebugId.ts
init_cjs_shims();
function injectSentryDebugId(rawSourceMap, debugId) {
  rawSourceMap.debugId = debugId;
  return rawSourceMap;
}

// src/integrations/UploadSentrySourcemap/mergeSourcemap.ts
init_cjs_shims();
var import_source_map = require("source-map");
async function mergeSourceMaps(packagerSourceMap, hermesSourceMap) {
  const packagerMapConsumer = await new import_source_map.SourceMapConsumer(packagerSourceMap);
  const hermesMapConsumer = await new import_source_map.SourceMapConsumer(hermesSourceMap);
  const generator = import_source_map.SourceMapGenerator.fromSourceMap(hermesMapConsumer);
  generator.applySourceMap(packagerMapConsumer, hermesMapConsumer.sources[0]);
  return generator.toJSON();
}

// src/integrations/UploadSentrySourcemap/uploadSourcemap.ts
init_cjs_shims();
var import_cli = __toESM(require("@sentry/cli"), 1);
function uploadSourcemap(bundlePath, sourcemapPath, options) {
  const client = new import_cli.default(null, options);
  return client.execute(["sourcemaps", "upload", "--debug-id-reference", bundlePath, sourcemapPath], true);
}

// src/integrations/UploadSentrySourcemap/UploadSentrySourcemap.ts
var UploadSentrySourcemap = class extends import_clipanion6.Command {
  static paths = [["sentry", "upload-sourcemap"]];
  apiKey = import_clipanion6.Option.String("--api-key", {
    required: false,
    description: "\uC778\uC99D API \uD0A4"
  });
  appName = import_clipanion6.Option.String("--app-name", {
    required: false,
    description: "\uC571 \uC774\uB984"
  });
  deploymentId = import_clipanion6.Option.String("--deployment-id", {
    required: false,
    description: "\uBC30\uD3EC ID"
  });
  sentryAuthToken = import_clipanion6.Option.String("--sentry-auth-token", {
    required: false,
    description: "Sentry \uC778\uC99D\uD0A4"
  });
  sentryOrg = import_clipanion6.Option.String("--sentry-org", {
    required: false,
    description: "Sentry \uC870\uC9C1"
  });
  sentryProject = import_clipanion6.Option.String("--sentry-project", {
    required: false,
    description: "Sentry \uD504\uB85C\uC81D\uD2B8"
  });
  baseUrl = import_clipanion6.Option.String("--base-url", {
    description: "API Base URL"
  });
  async execute() {
    const { baseUrl } = this;
    const apiKey = this.apiKey || await this.promptRequiredValue("password", "\uC571\uC778\uD1A0\uC2A4 API \uD0A4\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694", "API \uD0A4\uB294 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.");
    const appName = this.appName || await this.promptRequiredValue("text", "\uC571 \uC774\uB984\uC744 \uC785\uB825\uD574\uC8FC\uC138\uC694", "\uC571 \uC774\uB984\uC740 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.");
    const deploymentId = this.deploymentId || await this.promptRequiredValue("text", "\uBC30\uD3EC ID\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694", "\uBC30\uD3EC ID\uB294 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.");
    const sentryAuthToken = this.sentryAuthToken || process.env.SENTRY_AUTH_TOKEN || await this.promptRequiredValue("password", "Sentry \uC778\uC99D\uD0A4\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694", "\uC778\uC99D\uD0A4\uB294 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.");
    const sentryOrg = this.sentryOrg || process.env.SENTRY_ORG || await this.promptRequiredValue("text", "Sentry \uC870\uC9C1\uC744 \uC785\uB825\uD574\uC8FC\uC138\uC694", "\uC870\uC9C1\uC740 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.");
    const sentryProject = this.sentryProject || process.env.SENTRY_PROJECT || await this.promptRequiredValue("text", "Sentry \uD504\uB85C\uC81D\uD2B8\uB97C \uC785\uB825\uD574\uC8FC\uC138\uC694", "\uD504\uB85C\uC81D\uD2B8\uB294 \uD544\uC218 \uC785\uB825 \uD56D\uBAA9\uC785\uB2C8\uB2E4.");
    try {
      const zipPath = await downloadArtifacts({ apiKey, appName, deploymentId, baseUrl });
      const sources = await extractSources(zipPath);
      await p7.tasks([
        {
          title: "Android \uC18C\uC2A4\uB9F5 \uC5C5\uB85C\uB4DC \uC911...",
          task: async () => {
            await this.prepareSourceMapForUpload(sources.android);
            await uploadSourcemap(sources.android.hermesBundle, sources.android.hermesSourceMap, {
              authToken: sentryAuthToken,
              org: sentryOrg,
              project: sentryProject
            });
            return "Android \uC18C\uC2A4\uB9F5 \uC5C5\uB85C\uB4DC \uC644\uB8CC";
          }
        },
        {
          title: "iOS \uC18C\uC2A4\uB9F5 \uC5C5\uB85C\uB4DC \uC911...",
          task: async () => {
            await this.prepareSourceMapForUpload(sources.ios);
            await uploadSourcemap(sources.ios.hermesBundle, sources.ios.hermesSourceMap, {
              authToken: sentryAuthToken,
              org: sentryOrg,
              project: sentryProject
            });
            return "iOS \uC18C\uC2A4\uB9F5 \uC5C5\uB85C\uB4DC \uC644\uB8CC";
          }
        }
      ]);
      p7.outro("Sentry \uC18C\uC2A4\uB9F5 \uC5C5\uB85C\uB4DC \uC644\uB8CC");
    } catch (error) {
      if (error instanceof Error) {
        p7.log.error(error.message);
      } else {
        console.error(error);
      }
      process.exit(1);
    }
  }
  async prepareSourceMapForUpload(sources) {
    const debugId = await extractSentryDebugId(sources.packagerBundle);
    if (debugId == null) {
      throw new Error("Sentry Debug ID\uB97C \uCC3E\uC744 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4");
    }
    const mergedSourceMap = await mergeSourceMaps(
      await fs15.readFile(sources.packagerSourceMap, "utf-8"),
      await fs15.readFile(sources.hermesSourceMap, "utf-8")
    );
    const injectedSourceMap = injectSentryDebugId(mergedSourceMap, debugId);
    await fs15.writeFile(sources.hermesSourceMap, JSON.stringify(injectedSourceMap, null, 2), "utf-8");
  }
  async promptRequiredValue(type, message, validateMessage) {
    const result = await p7[type]({
      message,
      validate: (value) => {
        if (value.length === 0) {
          return validateMessage;
        }
        return;
      }
    });
    if (p7.isCancel(result)) {
      throw new Error("\uC791\uC5C5\uC774 \uCDE8\uC18C\uB418\uC5C8\uC5B4\uC694.");
    }
    return result;
  }
};

// src/index.ts
var import_web_config3 = require("@apps-in-toss/web-config");
function initialize() {
  const cli = new import_clipanion7.Cli({
    binaryLabel: "ait",
    binaryName: "ait"
  });
  cli.register(BuildCommand);
  cli.register(DeployCommand);
  cli.register(InitCommand);
  cli.register(MigrateCommand);
  cli.register(UploadSentrySourcemap);
  cli.register(TokenCommand);
  cli.register(TokenAddCommand);
  cli.register(TokenRemoveCommand);
  cli.register(import_clipanion7.Builtins.HelpCommand);
  cli.runExit(process.argv.slice(2));
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  babelBuild,
  babelJsonReplacementPlugin,
  getPackageManager,
  initialize,
  loadConfig
});
