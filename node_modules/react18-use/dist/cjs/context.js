"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useContext = exports.createContext = exports.getStoreContext = void 0;
const react_1 = __importDefault(require("react"));
const createStore = (defaultValue) => {
    let value = defaultValue;
    const listeners = new Set();
    const subscribe = (l) => {
        listeners.add(l);
        return () => listeners.delete(l);
    };
    const setValue = (nextValue) => {
        value = nextValue;
        listeners.forEach((l) => l());
    };
    const getValue = () => value;
    return { subscribe, setValue, getValue };
};
const STORE_CONTEXT = Symbol();
const getStoreContext = (context) => {
    const storeContext = context[STORE_CONTEXT];
    return storeContext;
};
exports.getStoreContext = getStoreContext;
exports.createContext = ((defaultValue) => {
    const context = react_1.default.createContext(createStore(defaultValue));
    const Provider = ({ value, children }) => {
        const storeRef = react_1.default.useRef(undefined);
        /* eslint-disable react-compiler/react-compiler */
        if (!storeRef.current) {
            storeRef.current = createStore(value);
        }
        const store = storeRef.current;
        /* eslint-enable react-compiler/react-compiler */
        react_1.default.useEffect(() => {
            store.setValue(value);
        });
        return react_1.default.createElement(context.Provider, { value: store }, children);
    };
    Provider.Provider = Provider;
    Provider[STORE_CONTEXT] = context;
    return Provider;
});
// LIMITATION:
// - Doesn't trigger rerender if the value is unchanged
exports.useContext = ((context) => {
    const storeContext = (0, exports.getStoreContext)(context);
    if (!storeContext) {
        throw new Error('useContext must be used with createContext from react18-use');
    }
    const store = react_1.default.useContext(storeContext);
    return react_1.default.useSyncExternalStore(store.subscribe, store.getValue);
});
