/**
 * @returns base - (nextSize - prevSize) / 2
 */
export declare const calculateOffset: ({ base, prevSize, nextSize }: {
    base: number;
    prevSize: number;
    nextSize: number;
}) => number;
