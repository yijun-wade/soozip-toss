import type { TouchableOpacityProps } from 'react-native';
export declare const IconButton: ({ name, accessibilityLabel, onPress, color, ...restProps }: {
    name: string;
    color?: string;
} & TouchableOpacityProps) => import("react/jsx-runtime").JSX.Element;
