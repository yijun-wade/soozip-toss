import type { Layout, RelativeTransformOrigin, TransformOrigin } from '../../../types';
export declare function getTransformOriginX(_value: TransformOrigin | RelativeTransformOrigin, { beforeValue, layout, }: {
    beforeValue: number;
    layout: Layout;
}): number;
