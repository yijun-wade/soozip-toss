import type { Layout, RelativeTransformOrigin, TransformOrigin } from '../../../types';
export declare function getTransformOriginY(_value: TransformOrigin | RelativeTransformOrigin, { beforeValue, layout, }: {
    beforeValue: number;
    layout: Layout;
}): number;
