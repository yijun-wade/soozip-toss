export interface CarouselContext {
    /** 캐러셀 아이템의 숫자 */
    itemsCount: number;
    /** 캐러셀 아이템의 너비 */
    itemWidth: number;
    /** 캐러셀 아이템 사이의 간격 */
    itemGap: number;
    /** 캐러셀 아이템을 둘러싸는 패딩 */
    padding: number;
}
